# Fieldcheck Command For OpenCode

Source command:

- `../../../dev-forge-skill/commands/fieldcheck.md`

OpenCode translation:

- Use the source command and docs as the field-consistency reference.
- Preserve report and issue output paths if generated.

