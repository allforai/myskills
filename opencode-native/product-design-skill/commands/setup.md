# Setup Command For OpenCode

Source command:

- `../../../product-design-skill/commands/setup.md`

OpenCode translation:

- The source file remains the reference for capability checks and key setup.
- OpenCode does not support Claude plugin registration semantics directly, so use
  this command wrapper as guidance rather than runtime parity.

