# Product Concept For OpenCode

Source workflow:

- `../../../product-design-skill/skills/product-concept.md`

OpenCode translation:

- Treat the source file as the canonical workflow.
- Convert `AskUserQuestion` checkpoints into normal user questions only when the
  missing answer blocks progress.
- Preserve any `.allforai/` outputs and decision logs described by the source.

