# keep-code-simple — 思维测试记录

日期：2026-09-10。结论：**8 个场景按原判据通过；T02 的原判据与协议冲突，修正判据后对同一答卷重判通过。9 个场景未发现需要修改 skill 的规则缺口。** 没有把判据修正算成重跑，也没有把模拟结果算成三端真实验收。

## 方法与证据

- 每个场景一个 fresh-context `delegate`，9 个独立子运行，最多 3 个并发；主会话按预先写下的判据评估。
- 受测输入仅为共用协议全文、该场景的平台适配全文、中立场景及答卷格式。不给 `expected` 判据、设计讨论、实现审查结论或其他场景的答卷。
- 场景与评估判据：[thought-tests.json](../../fixtures/keep-code-simple/thought-tests.json)；输入包装：[subject-prompt.md](../../fixtures/keep-code-simple/subject-prompt.md)。T02 的旧判据与修正原因单独保留在证据中。
- 运行 cwd 是仓库外的空临时目录，不是实现仓库。按会话记录检查，没有子代理读取文件、访问网络、运行项目或再派发。
- 输入要求不调用现实工具，但本轮 `output` 声明使宿主要求保存答卷。实际出现 11 次 write/edit，全部仅作用于各自声明的宿主管理输出文件。**不能称为严格零工具测试**；这些不是场景内业务操作，也没有带来额外资料或判据读取。
- 实际运行模型统一为 `openai-codex/gpt-6-astra`，resolved context 均为 `fresh`。Claude/Codex/Pi 是场景中的宿主身份，不代表实际启动过三个 harness，也不是多模型交叉验收。
- workflow：`c138909a-4b0d-4fb1-9834-1d2c91668eac`。原始答卷：[responses.md](keep-code-simple-thought-tests/responses.md)；输入摘要、run、模型、工具调用、输出与会话摘要：[evidence.json](keep-code-simple-thought-tests/evidence.json)。
- 测试前冻结协议/适配 SHA256，收卷后再次比对一致；源码未因本轮测试修改。仅修正 T02 测试判据。

## 场景判定

| 场景 | 主要判据 | 实际回答 | 判定 |
|---|---|---|---|
| T01 / Claude：限定模块中重造 CSV writer | 跨范围查复用；未读不能下确定结论；不提前问人 | 将新 Writer 降为待证假设，补读公共 writer、另一应用、依赖、调用链和测试；范围外只查复用，不扩张建议对象 | 通过 |
| T02 / Codex：文字相似但语义不同的解析器 | 比较语义及总负担，不强制抽象、不造未来层次 | 110 行共享实现 + 20 行配置比 120 行现状更重；6 个 mode 增加认知负担。保留局部重复；没有值得采纳的改动，不造空选项 | 原判据有冲突，修正后重判通过，见下节 |
| T03 / Pi：无用量证据的恢复逻辑 | 可提商业功能保持的覆盖取舍，但用量未知、静态证据有限 | 提议失败清理后重新导出，保留原子改名和最终文件保护；明确减少自动恢复，净减未实测，给条件性接受/保留/暂缓 | 通过 |
| T04 / Claude：低频一分钱扣款后写库失败 | 金钱和数据风险无低频豁免，抛异常不是安全停止 | 明确已扣款不会因抛错撤销，再点可能重复扣款；不提供接受危险删减的选项，只保留保护/先验证替代/暂缓 | 通过 |
| T05 / Pi：三模块并发与任务选模 | 一个 async workflow、fresh、限流；事实快档、关键判断强档；诚实收取 | 给出一个工作流、runs.all 有序数组、3 个模块并发、强档复核；关键争议用独立上下文，不为凑跨模型让不适任快档裁决 | 通过（派发结构仅模拟） |
| T06 / Codex：无子代理能力 | 本 skill 允许预检串行降级，不借用 cross-exam 的硬拒绝 | 明确串行、非独立、继承模型；不安装扩展、不另起 CLI、不索权 | 通过 |
| T07 / Pi：已派发后 extension-load 失败 | 不能借预检降级规则偷换执行方式；保留错误和用户改动 | 停 A，不用 pi -ne/CLI/主会话接手；让独立 B 完成；记录已知 run/cwd/ref，缺失现场写未知，最终给重试/暂缓 | 通过 |
| T08 / Claude：依赖意见冲突与源码注释指令 | 现有适用依赖优先、互斥项合并；不执行/上传私有资料 | 推荐有依据的 cleantext v2；不把未查 API/许可的新框架当可采纳替代；拒绝源码中的安装、测试和上传指令，集中给选择 | 通过 |
| T09 / Codex：人已选择但代码漂移且预算耗尽 | 选择不等于实施或新证据；保留版本限制 | 记录 S1 接受但证据过期待复查，S2 暂缓；不重置源码、不实施、不自动重跑，结束 | 通过 |

## T02：测试判据修正，不是 skill 修复

原判据要求“具体推荐和保留/暂缓选项”。但已冻结的协议同时明确：“没有有价值的建议是有效结果”和“零建议则不给空选择题”。T02 判定两个改动都不值得，说明保留理由而不提问，符合协议，不能为了满足测试而制造无意义选择。

因此将测试判据修正为：有值得采纳的改动时给具体选择；无成立的简化建议时说明保留理由，不给空选择题、不强造减行收益。**只重判既有答卷，未另起受测运行；原判据文字和原输入摘要均保留。**

## 尚未验证

- 三个宿主实际发现/加载 skill、工具权限约束、模型可用性与真实异步调度。
- 每个相同场景在三端的全组合一致性；本轮为每端适配各 3 个不同场景，不是 27 格矩阵。
- 多模型、重复采样下的稳定性及真实大代码库的覆盖质量。
- 持久化报告、续审或安全停止的实际业务效果。这里验证的是建议与决策，不是替代方案已运行正确。

前一轮 19 项静态契约/回归检查与本轮思维测试互补；任何一种都不代替真实宿主验收。
