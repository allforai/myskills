判定（本节点对这份读数）

- **类型不符，本节点不能判 done/gap。** 这份读数是上线后的产品行为指标（完成率、停留、券使用率、客服量），不是 runtime 证据。本能力的介质是 `runtime`，verdict ∈ {done, gap, unprovable}，判据是「release 产物在真环境启动后首次 API 2xx / 预期 4xx，且有 `served_by`、`readback`、`evidence.dir`（截图+网络日志+退出码）」。本读数一条都不具备：无构建标识、无启动记录、无首次请求抓包、无该 App HTTP client 实际用的 base URL。
- **故本节点给 `unprovable`，并声明 out of scope**：不是"跑过但失败"，是这类数据根本不构成本节点的证据。`overall` 不能因它变成 pass（entries 缺失/为空即拒，`overall` 不得为 pass）。
- 本节点是**上线前**的门（在 test harness 通过之后、launch-checklist 之前）；这份读数是上线后产物，时序上也不可能出自本节点。

对读数本身（只作信号，不作判定）

- **混染严重**：同一周另有「支付方式默认项」改动 + 10% 灰度「拼单」入口。三者并行，无法把 62.4%→38.1%、42s→71s 归因到「优惠券入口搬家」。客服 12→340 是最强信号，指向券的可发现性/引导回归，但仍是相关性，不是因果。
- 本节点只会在一种情况下覆盖它：入口搬家若伴随深链/路由改动导致**启动即 404/空白/崩溃**。读数是行为变化（停留变长、咨询变多），不是"启动打不到后端"，所以既不是本节点的 gap，也不能拿它替代 gap。

留给编排器 / 后续节点

1. `smoke-report.json` 不因这份读数新增 artifact 条目；若结算模块尚未 smoke，仍须按能力文件跑 release 构建 + 真环境启动 + 首次 API 抓包，`overall` 目前**缺 runtime 证据**，不可作为放行依据。
2. `contract_drift_warnings[]` 不适用（无 ≥2 处 env var 消费点证据），不要硬塞成漂移条目。
3. 把该读数路由给**产品指标/假设类节点**，并要求隔离变量（回滚单一改动或 A/B）后才谈因果；`launch-prep` 只能把它当"客服异常"旁证，不能当 smoke 通过。

Need from main agent：是否要把「客服 340 次」升级为线上事件处理（触发回滚/热修），这超出本节点权限，需编排器/用户决定。

No executor handoff warranted — 本节点不写文件、不产物；结论即上文。