**Inherited contract (visual-verify.md)**
- Reference must be a *source app*; without one the capability is skipped entirely. "设计稿" alone is ui-forge's reference, not visual-verify's — confirm what this round replicates.
- Coverage is not sampled: rule 10 + the gate expand the inventory into the frozen case matrix and refuse by name the first missing case ("用例未拍"). "抽样" can only mean *where reviewer attention is weighted*, never which cases are dropped.
- Pre-condition (rule 6): runs last — after cr-fidelity + product-verify + testforge, and after ui-forge when present; a defect ui-forge already closed must not come back as a visual-verify item (read its `fidelity-assessment.json`).

**Coverage (all 7 surfaces kept, no cap)**
1. 主页仪表盘 / 报表页 / 空状态 / 导出失败 / 设置页 / 登录页 / 全局错误页 — each is a surface row with non-empty axes; none annotated `not_applicable` without reason+basis.
2. Width axis from the *code census* (`layout_thresholds` path:line), straddling every threshold and reaching both ends of `width_range` (desktop → max ≥1920 unless `fixed_window`); the dev window is one value among them, never the axis.
3. Other axes per surface: appearance (`matchMedia` + `data-theme`), locale (`documentElement.lang`), dynamic type (root font size / `visualViewport.scale`), orientation, pointer.

**State injection (must be real, rule 5 tiers)**
4. 主页仪表盘: ViewModel/seeded data; report main path: seeded month → 图表 → 导出 CSV (composite milestone, layer 5); 空状态: ViewModel or network mock, **not** DOM rewriting (rule 1).
5. 导出失败: only tier-3 network mock is legitimate for server 500; the "手工断网试过一次" evidence does not count — it must be re-captured through the mock and read back.
6. If `.allforai/visual-verify/interaction-recordings.json` exists, replay the chain 选月份→看图表→导出CSV, not just screenshots (rule 8).

**Reviewer weighting (risk-ordered, not coverage-ordered)**
7. High attention: 报表主路径 (new), 空状态 (临时文案 — content diff vs source), 导出失败 (late-added, thin prior testing), responsive behaviour at the wide end.
8. Lower attention but still inspected: 设置页/登录页 (unchanged last round) — regression check only; dashboard — only visual-verify's categories, polish it owned by ui-forge stays out of scope.

**Records owed to the orchestrator / downstream**
9. Writes: `surface-inventory.json`, `case-matrix.json` (from `matrix.py`, verbatim), `visual-baseline.json` (each layout rule `pinned:false` or `pinned:true`+`ends`), `screenshot-manifest.json`, `evidence/<node_id>/`, `evidence-entries/<node_id>.json`, `visual-review-1.json/.md`, `visual-review-2.json`, `visual-verify-report.json`.
10. Node state comes from `check_visual_evidence.py`, not the node's own claim (`passed` / `refused` / `failed_visual_review`); any `high|medium` (`blocker|major`) finding fails it. Downstream hard consumers: `visual-review-1.json` state (pipeline-closure-verify), `screenshot-manifest.json` (launch-prep), `evidence-entries/<node_id>.json` (closure ledger) — these are `required`.

**Need from main agent:** is there a source app / accepted prior-round baseline to serve as the reference? If not, this node is skipped, and the "抽样方案" belongs to a different capability. Also confirm this run is `full` vs `analyze`.