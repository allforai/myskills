# 用户确认的视觉与交互基线

来源：现有设计文档、运行截图、用户逐类选择。

每类记录：候选规则 / 观察冲突 / 用户确认原意 / confirmed_at / 参考图路径 / 允许例外。

类别：direction、color、typography、layout、spacing、icons、components、navigation、feedback、states、motion、environment。

代表图必须标明构建、设备、OS、主题、字号、语言、方向、页面和状态。目标参考与当前截图分别标记。用户可以确认现有图的部分特征，不能把整张含缺陷截图隐式批准为 Golden。

环境矩阵使用具体可枚举值；每个页面声明适用状态和环境。确认后冻结为 JSON 并保存 SHA-256，验收中不得改写。
