Inherited decisions / constraints in play
- Solo dev (also sole support), bootstrap constraint: ship v1 within 3 weeks; budget ≈ 15 person-days vs 23.5 requested → prune is a hard scope gate, not a wishlist.
- Capability contract: every gap task gets an explicit `included` (no silent omissions); excluded tasks are preserved with reason (audit trail); at least one P0 must be included; `task_ref` is the FK.
- Default rule = frequency-first (P0/P1 by frequency unless effort prohibitive), but reasons must cite frequency/effort/value data — the T03 case is where blind frequency-first would be wrong.

`prune-tasks.json` → `decisions[]` (9/9 tasks, explicit)
```json
{"task_id":"T01","included":true,"tier":"P0","reason":"每周多次的核心动作，产品存在的前提；3d 在预算内，频次默认 P0"}
{"task_id":"T02","included":true,"tier":"P0","reason":"每周催款=相对 Excel 的差异化价值与收款闭环；2d 高性价比"}
{"task_id":"T03","included":true,"tier":"P1","reason":"一生一次但位于获客关键路径：目标用户多数从 Excel/别家迁入，无导入则首版无迁移通道；频次低估其价值，此处显式翻案"}
{"task_id":"T04","included":false,"tier":"P2","reason":"每月低频，Excel/CSV 导出已可覆盖，非发送-收款闭环必需"}
{"task_id":"T05","included":false,"tier":"P3","reason":"无海外用户即零需求，且 4d 为清单中最贵项，最差性价比"}
{"task_id":"T06","included":false,"tier":"P3","reason":"无调用方；开放 API 是长期承诺（鉴权/版本/文档），真实成本远超 3d"}
{"task_id":"T07","included":false,"tier":"P2","reason":"偶尔的审美偏好，非功能缺口；首版可用统一模板"}
{"task_id":"T08","included":false,"tier":"P2","reason":"成本/利润先需支出录入，真实范围远超 2d；与 T04 同属报表类，一并延后"}
{"task_id":"T09","included":false,"tier":"P3","reason":"纯偏好项；1.5d 虽便宜，但不服务首发目标，属可延后甜品"}
```
Records to leave for orchestrator / downstream nodes
- Coverage: 9/9 有显式决定，excluded 均保留原因（审计）；P0 已满足（T01/T02 included=true）。
- Budget: included = T01+T02+T03 = 8d ≤ 15d，留 ~7d 给联调、上线与未估项 —— 有意不填满，避免"默认全收"。
- 本节点契约仍未闭合：还需 `frequency-tier.json` 与 `prune-report.md`（能力文件列为必备产物）。只交 decisions 不能视为节点完成，编排器需补产/校验。
- Pending confirm (orchestrator): T03 是对 frequency-first 的显式例外；若用户明确不以"从 Excel 迁移"为获客路径，则 T03 退回 excluded。
- Uncertainty: 上游估工是乐观值（T06/T08 明显低估，实为多周级）；本次裁剪对 T04–T09 是"延后"而非"砍掉"，未来版本重新进入 gap。
- Downstream: translate / generate-artifacts 只能实现 `included=true`（T01–T03）；product-verify 可引用排除原因解释缺失功能。

No executor handoff is warranted — this is a one-shot scope decision, and the two remaining contract artifacts (`frequency-tier.json`, `prune-report.md`) need the orchestrator/main agent to produce or authorize, which this node did not do.