# Deterministic workflow simulation

Workflow revision2 adds tasks/role-simulation.json, generated by actual controller.py dry-run. T9/T15 packet assembly checks all22 referenced file hashes; T15 includes the new-product cell. The T15/T16/T17 waiting schedule admits only T15, runs/release both subjects, releases evaluator, runs/release supervisor and the parallel review pair, then admits T16. Admission assertions reserve auxiliary capacity and prioritize pending completion work. This deterministic progress proof is not real task/runtime evidence. Product DAG and paths remain unchanged.

Spec2 / task1 / workflow1. validate_plan_tasks passed ten tasks. build_artifact_contracts froze exact paths and acceptance hashes. build_task_dag and simulate_workflow both returned ok. All ten tasks reachable, initial ready T9, informational maximum ready width3. No declared resource mutex; isolated temporary fixtures are mandatory and any discovered shared physical target must acquire a named mutex.

The path collision group T11/T12/T13 is a serialized merge/replay obligation, not an implementation mutex. Readiness is recomputed after each verified integration marker, never held behind informational wave barriers. Full dependencies and each failure blast radius are in tasks/workflow-simulation.json; that machine result is the sole copy.

This is a no-dispatch simulation, not implementation or scenario evidence. Workflow reverse closure and launch remain pending.
