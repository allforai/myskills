# 页面交互类型体系 v2

> 本文档是页面交互类型的**单一事实来源**（Single Source of Truth）。
> 各阶段技能引用本文档，不重复定义类型。

## 引用关系

```
docs/interaction-types.md（本文档 — 定义层）
  ↑ 引用
  ├── screen-map.md     → Step 1 推断 interaction_type，写入 screen-map.json
  ├── ui-design.md      → Step 4 按 interaction_type 生成视觉规格
  └── design-to-spec.md → Step 2 按 interaction_type 生成实现规格
```

---

## 核心设计原则

### 原则一：行为层与表现层分离

**交互类型 = 行为契约**，只描述：
- 用户意图（用户想达成什么目标）
- 操作集合（用户能执行哪些操作）
- 数据状态（实体有哪些状态和流转规则）
- 信息流向（数据如何在用户和系统间流动）

**UI = 表现决策**，放在平台实现矩阵中，不属于类型定义。

> ❌ 错误：`布局：左侧树 + 右侧编辑区`（这是 UI）
> ✅ 正确：`实体有父子层级关系，操作需携带 parentId 上下文`（这是行为）

### 原则二：类型平台无关

同一交互类型在不同平台的 UI 实现可以完全不同，行为契约相同。

### 原则三：CRUD 的 U 有两种子形态

| 子形态 | 定义 | 典型操作 |
|--------|------|---------|
| 字段编辑（Edit） | 用户自由修改实体字段值 | 修改商品名称、价格、描述 |
| 状态流转（State Transition） | 触发预定义操作，实体状态跳转，无字段编辑 | 商品上架/下架、订单发货/取消 |

---

## 三轴模型总览

```
第一轴：交互类型（行为定义层）
  37 种类型，8 个分类（MG/CT/EC/WK/RT/SB/SY/TU）
  每种类型：用户意图 + 操作集合 + 数据状态 + 信息流向

  ×

第二轴：平台实现矩阵（行为 → UI 映射）
  5 个平台：Web桌面 / Mobile Web / iOS+Android / Windows / TUI
  每种类型 × 每个平台：导航模式 / 内容展示 / 操作触发 / 写操作承载 / 平台特有能力

  ×

第三轴：上下文预设（影响类型分布与风格倾向）
  产品类型：电商 / 社交 / IM / 社区 / 工具 / 办公 / 金融 / 内容
  用户属性：C端消费者 / B端商户 / 内部办公
```

### 类型代号体系

| 前缀 | 分类 | 数量 | 代号范围 |
|------|------|------|---------|
| MG | 管理/CRUD | 9 | MG1–MG8（含 MG2 子类型） |
| CT | 内容消费 | 8 | CT1–CT8 |
| EC | 电商交易 | 3 | EC1–EC3 |
| WK | 协作办公 | 7 | WK1–WK7 |
| RT | 通讯实时 | 4 | RT1–RT4 |
| SB | 审核提交 | 1 | SB1 |
| SY | 引导系统 | 2 | SY1–SY2 |
| TU | TUI/CLI | 4 | TU1–TU4 |

### screen-map 标注格式

单一类型：
```json
{ "interaction_type": "MG3" }
```

组合类型（用数组，首元素为主类型）：
```json
{ "interaction_type": ["MG5", "MG3"] }
```

---

## 第一轴 + 第二轴：交互类型定义与平台矩阵

### MG 管理/CRUD 类

#### MG1 只读列表

**用户意图：** 浏览、筛选、查找数据，无修改意图。

**操作集合：** 查询（分页/排序）、多维筛选、搜索、导出、点击进入详情。

**数据状态：** 无状态机，数据只读。

**信息流向：** `GET list(params)` → 展示 → 可选 `GET detail(id)` 跳转。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 进入方式 | 侧边栏菜单 | 底部 Tab / 顶部返回 | 底部 Tab / 导航栈 | 左侧导航树 | TU2 菜单选择 |
| 内容展示 | 多列数据表格 | 单列卡片列表（2-3 字段） | UITableView / RecyclerView | DataGrid 控件 | 格式化文本表格 |
| 筛选操作 | 顶部 Filter Bar 常驻 | 底部 Sheet 展开筛选 | SearchController + Sheet | 顶部筛选工具栏 | `--filter` 参数 |
| 分页方式 | 分页器（Pagination） | 上拉加载更多 | 下拉刷新 + 上拉加载 | 分页器 | 分批输出 / `--limit` |
| 导出 | 工具栏按钮 | 无 / 系统分享 | 系统分享 Sheet | 菜单栏 文件→导出 | `> output.csv` 重定向 |
| 平台特有 | 多列排序点击列头 | 触摸友好大行高 | 系统搜索集成 | 键盘快捷键导航 | ANSI 颜色高亮字段 |

---

#### MG2 CRUD 实体集群

**用户意图：** 对某类实体进行完整生命周期管理（创建、查看、修改、删除）。

**操作集合：** 查询列表、查看详情、新建实体、编辑字段、状态流转、删除实体（含确认）、可选批量操作。

**数据状态：** 实体存在/不存在；可含子状态（如启用/禁用）。

**信息流向：** `GET list` / `GET detail(id)` / `POST create` / `PUT update(id)` / `DELETE(id)` / 状态操作 `POST action(id)`。

MG2 实体集群由以下子屏组成，screen-map 可精确标注子类型：

| 子屏 | 代号 | 行为摘要 |
|------|------|---------|
| 实体列表 | MG2-L | 可分页查询，提供操作入口，支持批量操作 |
| 实体详情 | MG2-D | 只读展示完整字段，触发操作按钮 |
| 新建表单 | MG2-C | 空表单 → 提交 → 创建实体 |
| 字段编辑 | MG2-E | 表单回填旧值 → 修改 → 提交更新（**必须回填**） |
| 状态流转 | MG2-ST | 触发特定操作使实体状态跳转，无字段编辑表单 |

**平台矩阵（以 MG2-L 列表为基准）：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 新建操作 | 工具栏按钮 | 右下角 FAB | 导航栏右侧按钮 | 工具栏按钮 | `create` 子命令 |
| 编辑承载 | 字段少→弹窗 / 字段多→独立页 | 独立页（全屏） | 独立页（push） | 弹窗 / 独立页 | 交互式表单（TU2） |
| 删除确认 | Modal 确认框 | Alert 弹层 | UIAlertController | Dialog | `y/N` 提示 |
| 状态流转触发 | 操作列下拉菜单 | 长按 / 底部操作栏 | Swipe Actions / 操作 Sheet | 右键菜单 | `action` 子命令 |
| 批量操作 | Checkbox + 工具栏 | 无 / 编辑模式简化 | iOS 编辑模式 | Checkbox + 菜单 | `--ids` 多值参数 |
| 平台特有 | 行内快速编辑 | 触摸长按预览 | Haptic 触感反馈 | 键盘快捷键 CRUD | 管道批量处理 |

---

#### MG3 状态机驱动

**用户意图：** 对实体执行业务操作，驱动其在预定义状态间流转（无自由字段编辑）。

**操作集合：** 仅状态操作（如发货/取消/冻结/恢复/审核通过），每个操作对应合法的状态跳转；带输入的操作（如拒绝需填理由）通过简单的确认表单完成。

**数据状态：** 实体有有限状态集（如 pending/active/frozen/cancelled），操作只能触发合法跳转。

**信息流向：** `GET list(status_filter)` → 当前状态决定可用操作 → `POST action(id)` → 服务端状态变更 → 刷新列表。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 状态展示 | 状态标签（带颜色语义） | 状态标签（带颜色） | 状态标签 / SF Symbol | 图标 + 文字 | ANSI 颜色文字 |
| 操作触发 | 操作列下拉菜单（按当前状态条件渲染） | 底部操作栏（滑出） | Swipe Actions / Action Sheet | 右键菜单 | TU2 交互式菜单 |
| 带输入操作 | Modal 小表单（如填驳回原因） | 全屏简单表单 | UIAlertController 文本输入 | Dialog 文本框 | 提示符输入 |
| 批量状态操作 | Checkbox + 工具栏批量按钮 | 无 / 简化 | 编辑模式 | Checkbox + 菜单 | `--ids` 参数 |
| 状态筛选 | 顶部状态 Tab / 下拉筛选 | 横向滚动状态 Tab | 顶部 Segment 控件 | 下拉筛选 | `--status` 参数 |
| 平台特有 | 状态变更后行内刷新（无整页刷新） | 操作后 Toast 提示 | Haptic 反馈 + 动画 | 状态栏提示 | 退出码表示结果 |

---

#### MG4 审批流

**用户意图：** 审查他人提交的内容或申请，做批准或驳回决策。

**操作集合：** 浏览待审队列（默认过滤待审状态）、查看详情、批准、驳回（需填驳回原因）、可选批量审批。

**数据状态：** 待审核 → 已通过 / 已驳回（不可逆）。

**信息流向：** `GET list(status=pending)` → `GET detail(id)` → `POST approve(id)` / `POST reject(id, reason)` → 通知提交方。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 队列展示 | 默认筛选待审状态的列表 | 待审列表（卡片） | 待审列表（UITableView） | 待审列表（DataGrid） | `--status=pending` 过滤列表 |
| 进入详情 | 列表行点击或「审核」链接 | 点击卡片全屏详情 | push 到详情页 | 列表行双击 | `view <id>` 子命令 |
| 批准操作 | 详情页按钮 | 底部固定按钮 | 底部操作栏 | 工具栏按钮 | `approve <id>` 命令 |
| 驳回操作 | 弹窗表单（填驳回原因） | 全屏填写驳回原因 | Alert + 文本输入 | Dialog | `reject <id> --reason "..."` |
| 批量审批 | Checkbox + 批量操作工具栏 | 无（移动端不适合批量） | 编辑模式（可多选） | Checkbox + 菜单 | `approve --ids 1,2,3` |
| 平台特有 | 键盘快捷键（A批准/R驳回） | 下拉刷新队列 | 推送通知提醒新待审 | 系统通知 | cron 脚本定时审批 |

---

#### MG5 主从详情

**用户意图：** 查看一个实体的完整信息，并浏览其关联的多类子实体数据。

**操作集合：** 查看主实体信息、切换子实体分类（如订单/积分/评价）、在子实体上执行各自的操作、可对主实体执行状态操作。

**数据状态：** 主实体状态 + 各子实体列表各自独立（分别分页）。

**信息流向：** `GET entity(id)` → 按需 `GET entity_orders(id, params)` / `GET entity_points(id, params)`（懒加载各子表）。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 主实体展示 | 顶部信息区（固定，不随滚动消失） | 折叠式头部（向上滚动时收起） | 固定头部 View | 顶部面板 | 首屏关键字段展示 |
| 子实体切换 | 水平 Tab 导航 | 水平滚动 Tab | UITabBarController / ViewPager | 左侧树节点 | `--section orders` 参数 |
| 子实体列表 | 各 Tab 独立分页表格 | 各 Tab 独立上拉加载 | 各 Tab 独立 UITableView | 各节点独立列表 | 分节标题 + 列表 |
| 主实体操作 | 顶部操作按钮 | 底部操作栏 / 顶部导航按钮 | 导航栏右侧按钮 / Bottom Bar | 工具栏 | `action <id>` 子命令 |
| 路由格式 | `/entities/:id` | `/entities/:id` | 导航栈参数传递 | 导航路径参数 | `view entity <id>` |
| 平台特有 | 子表内嵌操作不触发页面跳转 | 子操作全屏展示 | 子操作 push 新页 | 子操作弹窗 | 子实体交互式菜单 |

---

#### MG6 树形管理

**用户意图：** 管理具有父子层级关系的实体结构，维护层级 CRUD。

**操作集合：** 展开/折叠节点、新建节点（需指定父节点，即携带 parentId）、编辑节点、删除节点（需处理子节点，删除前确认）、移动节点（变更 parentId）。

**数据状态：** 树结构，节点有深度、路径、子节点数；任何写操作后树状态变更。

**信息流向：** `GET tree()` 加载完整树 → 写操作（`POST createNode({parentId})` / `PUT updateNode(id)` / `DELETE node(id)`）→ 重新加载完整树。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 树结构呈现 | 可展开折叠的树形组件 | 缩进列表（accordion） | UIOutlineView / 缩进 RecyclerView | TreeView 控件 | 字符树形（`├──` 等） |
| 选中节点 | 点击节点高亮，右侧编辑区联动 | 点击展开操作菜单 | 点击 push 详情/编辑 | 点击节点激活右侧面板 | 方向键导航，回车选中 |
| 新建子节点 | 选中父节点 → 点击新建按钮 → 表单 | 长按父节点 → 新建选项 | 操作 Sheet → 新建子节点 | 右键菜单 → 新建子节点 | `create --parent-id <id>` |
| 删除含子节点 | Modal 确认（说明子节点处理方式） | Alert 确认 | UIAlertController | Dialog | `y/N` + 子节点处理选项 |
| 节点移动 | 拖拽（可选）/ 剪切粘贴 | 不支持拖拽，通过菜单移动 | 长按拖拽（可选） | 拖拽 / 右键剪切 | `move <id> --to-parent <id>` |
| 平台特有 | 键盘展开/折叠（空格/方向键） | 触摸友好大节点高度 | 系统级拖拽支持 | 键盘快捷键 Ctrl+X/V | 完整可视化树管理器（如 `ranger`） |

---

#### MG7 仪表盘

**用户意图：** 获取业务全局数据视图，辅助决策，无需修改数据。

**操作集合：** 时间范围选择、数据钻取（下探到明细）、图表交互（hover/点击区域）、定时刷新（可选）。无写操作。

**数据状态：** 无状态机，纯聚合读取，数据有时效性。

**信息流向：** 并行 `GET statistics(params)` × N → 聚合展示；用户调整筛选条件 → 重新拉取。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 整体密度 | 高密度网格（多图表并排） | 垂直堆叠单列（每次展示一个图表） | 垂直滚动列表 | 可调整大小的多面板 | 字符图表（sparkline / 柱状） |
| 时间筛选 | 顶部日期范围选择器 | 底部 Sheet 时间选择 | DatePicker Sheet | 下拉日历 | `--from` / `--to` 参数 |
| 图表交互 | hover 显示 tooltip，点击钻取 | 触摸显示 tooltip | 触摸 tooltip，点击钻取 | hover tooltip | 无交互（纯输出） |
| 刷新策略 | 定时轮询 / 手动刷新按钮 | 下拉刷新 | 下拉刷新 | 自动刷新 + 状态栏 | `--watch` 定时重执行 |
| 平台特有 | 多窗口多仪表盘对比 | 关键指标卡片化（适应小屏） | 系统 Widget 支持 | 多显示器跨屏 | 可集成 CI/CD pipeline |

---

#### MG8 配置页

**用户意图：** 查看并修改单一实体的配置或设置，一次性提交生效。

**操作集合：** 加载当前配置值、修改字段（**必须回填已有值**）、整体保存或分区保存、可选重置为默认值。

**数据状态：** 已保存 / 修改中（本地 dirty 状态，未保存前不影响服务端）。

**信息流向：** `GET settings()` 加载 → 用户修改（本地 dirty）→ `PUT settings(values)` 保存 → 确认成功。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 表单结构 | 单页分组表单（分 Section） | 单页垂直滚动分组 | UITableView grouped style | 分组 Panel | 分区交互式表单 |
| 保存操作 | 顶部/底部「保存」按钮 | 底部固定「保存」按钮 | 导航栏右侧「储存」 | 工具栏「保存」 | `set key=value` 或 `apply` 命令 |
| 离开确认 | 有未保存修改时浏览器 beforeunload | 返回时 Alert 提示 | pop 前 UIAlertController 确认 | 关闭窗口确认 Dialog | 退出前 `y/N` 提示 |
| 重置默认 | 「恢复默认」链接/按钮 | 底部危险操作区 | 底部危险区 | 「恢复默认」菜单项 | `reset` 子命令 |
| 实时预览 | 可选（如主题配置即时预览） | 无（网络成本高） | 有（如头像裁剪） | 有（如界面设置） | 无 |
| 平台特有 | 多标签页分区配置 | 简化字段（仅必要项） | 系统设置风格 | 注册表风格嵌套 | 配置文件导入导出 |

### CT 内容消费类

#### CT1 Feed 流/信息流

**用户意图：** 在持续更新的内容流中发现感兴趣的内容，进行即时互动。

**操作集合：** 向下滚动加载更多、点赞/收藏/分享、评论、关注/取关作者、举报内容。

**数据状态：** 无限列表（游标分页），每条内容有互动计数（点赞数/评论数）状态。

**信息流向：** `GET feed(cursor)` → 渲染内容卡片 → 用户互动 `POST like(id)` / `POST follow(userId)` → 本地计数更新。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 滚动方式 | 鼠标滚轮垂直滚动 | 手指触摸垂直滚动 | 手指触摸垂直滚动 | 鼠标滚轮 | 方向键 / `j/k` 翻页 |
| 加载策略 | 滚动接近底部自动加载 | InfiniteScroll + 加载指示 | InfiniteScroll + 下拉刷新 | 滚动到底自动加载 | 按键触发加载下一批 |
| 互动操作触发 | hover 显示操作按钮 | 内容卡片内固定操作区 | 内容卡片内固定操作区 | hover 显示操作 | 选中内容后数字键操作 |
| 平台特有 | 多列瀑布流布局选项 | 触摸双击快速点赞 | Haptic 反馈 + 长按预览 | 无（少见场景） | 文本摘要 + URL，无图片 |

---

#### CT2 内容阅读页

**用户意图：** 完整阅读单篇文章、帖子或长内容，可互动但不编辑。

**操作集合：** 滚动阅读、点赞/收藏/分享、关注作者、查看/发表评论、举报。

**数据状态：** 只读内容 + 互动计数 + 评论列表（分页）。

**信息流向：** `GET article(id)` → 渲染全文 → 评论区独立 `GET comments(articleId, page)` 懒加载。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 阅读进度 | 顶部进度条（可选） | 顶部进度条 | 顶部进度条 | 滚动条 | 行号 / 百分比提示 |
| 字体控制 | 浏览器缩放 | 无（系统字体） | 系统辅助功能字体 | 窗口缩放 | 终端字体大小 |
| 评论区 | 文章下方加载 | 文章下方 + 上拉加载 | 文章下方 / 独立评论页 | 文章下方 | `--comments` 参数查看 |
| 互动操作 | 顶部 / 底部固定操作栏 | 底部固定操作栏 | 底部操作栏 | 工具栏 | 命令操作 |
| 平台特有 | 划词评论（部分平台） | 阅读模式（浏览器） | 系统分享 Sheet | 打印支持 | 可 pipe 给 `less` 分页 |

---

#### CT3 个人主页/Profile

**用户意图：** 了解某用户的身份信息、内容产出和社交关系。

**操作集合：** 关注/取关、发私信、查看不同类型内容（动态/作品/收藏）、举报用户。

**数据状态：** 关注关系状态（未关注/已关注/互相关注）；内容列表按类型分组。

**信息流向：** `GET user(id)` → 渲染用户信息 + 关注状态 → 按 Tab 懒加载各类型内容列表。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 用户信息区 | 顶部固定区域（头像/简介/数据） | 顶部可折叠区域 | 顶部固定 Header View | 顶部面板 | 首屏文本信息卡 |
| 内容切换 | 水平 Tab 导航 | 水平滚动 Tab | UISegmentedControl / ViewPager | 左侧内容分类列表 | `--tab works` 参数 |
| 关注操作 | 按钮点击（状态即时切换） | 按钮点击 | 按钮点击 + Haptic | 按钮点击 | `follow <userId>` 命令 |
| 平台特有 | 悬停显示用户卡片预览 | 长按头像查看大图 | 系统联系人集成（可选） | 无 | 纯文本信息展示 |

---

#### CT4 卡片探索（Swipe 模式）

**用户意图：** 快速浏览候选内容/用户，通过滑动手势表达喜欢或跳过的意向。

**操作集合：** 右滑（喜欢/匹配）、左滑（跳过）、点击查看详情、撤销上一步操作（可选）、超级喜欢（可选）。

**数据状态：** 候选队列（预加载），每次决策消耗一张卡片，队列耗尽后自动补充。

**信息流向：** 预加载候选队列 `GET candidates()` → 用户决策 `POST like(id)` / `POST skip(id)` → 队列消耗 → 自动补充下一批。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 决策操作 | 点击屏幕左/右半区，或键盘方向键 | 触摸横向滑动手势 | 原生触摸横向滑动（Gesture Recognizer） | 鼠标点击左/右按钮 | N/A（手势无法映射到纯键盘，不适用） |
| 卡片动效 | CSS 旋转拖拽动画 | 原生弹簧物理动画 | UIKit / Compose 物理动画 | WPF 动画 | N/A |
| 撤销 | 按钮点击 | 摇一摇 / 按钮 | 摇一摇 / 按钮 | 按钮 | N/A |
| 平台特有 | 鼠标拖拽模拟滑动 | 最接近原生体验 | 最佳体验平台 | 体验受限 | N/A（核心交互依赖触摸手势） |

---

#### CT5 媒体播放器

**用户意图：** 播放视频或音频内容，控制播放进度和体验。

**操作集合：** 播放/暂停、拖拽进度条（seek）、调节音量、切换全屏/小窗/画中画、切换清晰度、开关字幕、开关弹幕。

**数据状态：** 播放状态机（加载中/播放中/暂停/缓冲中/结束/错误）；播放进度；音量。

**信息流向：** 获取媒体 URL → 流媒体加载 → 播放状态机驱动控件显隐 → 进度事件上报 → 播放记录写回服务端。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 播放控件 | hover 显示控制栏（底部） | 点击显示控制栏（底部） | 点击显示控制栏 | 固定底部控制栏 | 音频：字符进度条 + 键盘控制（如 cmus）；视频：N/A |
| 全屏 | F11 / 按钮 | 横屏自动 / 按钮 | 横屏 + AVPlayerViewController | 窗口最大化 / 按钮 | N/A（视频不适用） |
| 画中画 | 浏览器原生 PiP | 浏览器 PiP（部分支持） | 系统级 PiP | 应用自实现 PiP | N/A |
| 弹幕 | 视频上方滚动文字层 | 同上（移动优化密度） | 同上 | 同上 | N/A |
| 平台特有 | 键盘快捷键（Space/左右箭头） | 双击快进/快退（±10s） | AirPlay / Chromecast | 系统媒体键支持 | cmus 等 TUI 音乐播放器 |

---

#### CT6 相册/图库

**用户意图：** 浏览图片集合，查看感兴趣图片的大图。

**操作集合：** 网格浏览、点击进入大图、左右切换大图、下载/保存、分享、（可选）删除。

**数据状态：** 图片列表 + 当前查看的图片索引。

**信息流向：** `GET images(albumId, page)` → 网格展示缩略图 → 点击 `GET imageDetail(id)` → 大图查看器。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 网格展示 | 固定列数网格（如 3-4 列） | 自适应列数（2-3 列） | UICollectionView 网格 | WrapPanel 网格 | 文件名列表 + 编号 |
| 大图查看 | 弹出层全屏查看 | 全屏覆盖层 | 全屏 UIImageView / PhotoView | 弹出窗口全屏 | 在终端输出图片（如 `viu`）或 N/A |
| 切换图片 | 键盘左右箭头 / 两侧按钮 | 横向滑动手势 | 横向滑动（UIPageViewController） | 键盘左右箭头 | `n/p` 下一张/上一张 |
| 缩放 | 鼠标滚轮 / 双击 | 双指 pinch 缩放 | 双指 pinch 缩放（系统级） | Ctrl+滚轮 | N/A |
| 平台特有 | 键盘导航整个相册 | 系统相机直接集成 | 系统相册深度集成 | 文件浏览器联动 | ASCII Art 模式（如 `catimg`） |

---

#### CT7 搜索结果页

**用户意图：** 基于关键词找到符合多维条件的内容或实体。

**操作集合：** 输入/修改关键词、应用/清除多维筛选条件、切换排序方式、翻页/加载更多、点击进入详情。

**数据状态：** 搜索会话状态（关键词 + 筛选条件集合 + 排序 + 页码），URL 参数化以支持分享/历史。

**信息流向：** 搜索参数 → `GET search(q, filters, sort, page)` → 带高亮关键词的结果列表。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 搜索入口 | 顶部搜索框（常驻） | 顶部搜索框 + 软键盘 | 顶部 UISearchController | 顶部搜索框 | 命令参数 `search "keyword"` |
| 筛选面板 | 左侧固定筛选面板 | 底部 Sheet 展开 | 底部 Sheet / 筛选页面 | 左侧面板 | `--filter key=value` 参数链 |
| 结果展示 | 列表或网格（可切换） | 列表（垂直） | 列表 / 网格 | 列表或网格 | 格式化文本列表 |
| 关键词高亮 | 结果文本中高亮匹配词 | 同上 | 同上 | 同上 | ANSI 颜色高亮 |
| 平台特有 | 搜索历史下拉建议 | 软键盘搜索按钮 | 系统 Spotlight 集成（可选） | 全局搜索集成 | 正则表达式支持 |

---

#### CT8 Story/短视频流

**用户意图：** 快速刷短视频或 Story 内容，即时娱乐，连续消费。

**操作集合：** 向上滑动切换到下一条、向下滑动返回上一条、点赞/评论/分享、关注创作者、点击互动贴纸。

**数据状态：** 当前播放视频索引，视频自动播放，滑动触发切换。

**信息流向：** 预加载前后 N 条视频（HLS/MP4）→ 用户滑动 → 切换播放 → 互动写回 → 推荐算法更新队列。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 切换方式 | 鼠标滚轮 / 键盘上下键 | 触摸垂直滑动手势 | 原生触摸垂直滑动（SnapPager） | 鼠标滚轮 | N/A（全屏竖向视频依赖触摸，不适用） |
| 播放控制 | 点击暂停/播放 | 点击暂停/播放 | 点击暂停/播放 | 点击暂停/播放 | N/A |
| 互动操作 | 右侧固定操作区 | 右侧固定操作区 | 右侧固定操作区（系统级） | 右侧固定操作区 | N/A |
| 预加载策略 | 提前加载 2-3 条 | 提前加载 1-2 条（流量敏感） | 提前加载 2-3 条（后台预缓存） | 提前加载 2-3 条 | N/A |
| 平台特有 | 体验次于移动端（少见场景） | 接近原生体验 | **最佳体验平台**（原生设计场景） | 体验受限 | N/A（核心依赖触摸 + 全屏） |

### EC 电商交易类

#### EC1 商品详情页

**用户意图：** 全面了解商品信息，选择规格，决策是否购买。

**操作集合：** 浏览图片、选择规格/SKU（影响价格和库存）、设置购买数量、加入购物车、立即购买、收藏商品、查看评价列表。

**数据状态：** 规格选择状态（已选/未选各维度），选定 SKU 对应的价格/库存实时联动；加购成功状态。

**信息流向：** `GET product(id)` 加载商品基础信息 + SKU 矩阵 → 用户选择规格触发本地 SKU 匹配（价格/库存更新）→ `POST cart/add({skuId, qty})` 加购。

> **与 MG5 主从详情的区别：** EC1 的核心行为是「规格选择联动价格/库存」和「加购决策」，不是子实体管理；数据流以购买决策为终点，而非信息浏览。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 图片浏览 | 主图 + 缩略图列表，鼠标 hover 放大 | 横向滑动图片轮播 | 横向滑动（UIPageViewController） | 图片轮播组件 | N/A（图片无法在终端完整呈现，不适用） |
| 规格选择 | 点击选项按钮（多行多列） | 点击选项按钮（横向滚动） | 点击选项按钮 | 点击选项按钮 | N/A |
| 加购/购买 | 固定在视口右侧 / 底部操作区 | 底部固定操作栏 | 底部固定操作栏（系统级安全区） | 固定操作区 | N/A |
| 评价区 | 页面下方，抽样展示 + 查看全部 | 页面下方，折叠展示 | 独立 Tab 或折叠区 | 页面下方 | N/A |
| 平台特有 | 多图对比（部分平台） | 触摸 pinch 图片缩放 | AR 试穿/试用（部分品类） | 无 | N/A（核心依赖图片和规格选择 UI） |

---

#### EC2 购物车/结算清单

**用户意图：** 管理待购买商品，确认数量和价格，准备结算。

**操作集合：** 选中/取消选中商品（影响结算范围）、修改商品数量（实时更新价格）、删除商品、应用优惠券/促销码、查看运费、进入结算。

**数据状态：** 商品列表（含各商品选中状态）、实时价格汇总（随选中/数量变化实时重算）。

**信息流向：** `GET cart()` 加载购物车 → 本地状态变更（选中/数量）→ 实时重算价格（本地计算或 `POST cart/estimate`）→ 「结算」触发 `POST order/create(selectedItems)`。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 商品列表 | 垂直列表，每行含图片/名称/规格/数量/价格 | 垂直列表（简化，图片较小） | UITableView 垂直列表 | 垂直列表 | N/A（购物车依赖图片和复杂交互，不适用） |
| 数量调整 | 数字输入框 / ±按钮 | ±按钮（触摸友好） | ±按钮（触摸友好） | 数字输入框 / ±按钮 | N/A |
| 价格汇总 | 底部固定汇总区（随选中实时更新） | 底部固定汇总区 | 底部固定汇总区（安全区） | 底部汇总区 | N/A |
| 优惠券 | 输入框 / 下拉选择 | 点击展开优惠券列表 | 优惠券选择页 | 输入框 | N/A |
| 平台特有 | 键盘 Tab 快速切换商品 | 侧滑删除商品 | 侧滑删除（UISwipeAction） | 右键删除 | N/A（核心交互依赖触摸和视觉） |

---

#### EC3 订单追踪时间线

**用户意图：** 了解订单当前状态和物流配送进展，无需执行任何操作。

**操作集合：** 查看时间线节点（只读）、可选点击物流节点查看详细位置信息。**无写操作。**

**数据状态：** 只读时间线节点集合（如已下单/已付款/已发货/运输中/派送中/已签收），每个节点有时间戳和描述。

**信息流向：** `GET order(id)` 获取订单基础状态 + `GET logistics(trackingNo)` 获取物流轨迹 → 合并展示时间线。

> **与 MG3 状态机驱动的区别：** EC3 无任何操作触发，纯只读展示历史轨迹；MG3 有用户触发的状态操作。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 时间线展示 | 垂直步骤条（已完成节点高亮） | 垂直步骤条（全宽显示） | UIProgressView + 步骤列表 | 步骤条组件 | 字符时间线（`✓ 2024-01-01 已下单`） |
| 当前状态 | 当前节点高亮 + 动画（可选） | 当前节点高亮 | 当前节点高亮 + 动画 | 当前节点高亮 | `→` 指示当前状态 |
| 物流地图 | 可嵌入地图显示快递员位置（可选） | 可嵌入地图 | 原生地图集成（可选） | 可嵌入地图 | 文本地址信息 |
| 刷新 | 手动刷新按钮 / 自动轮询 | 下拉刷新 | 下拉刷新 | 手动刷新 | `--watch` 定时重查 |
| 平台特有 | 多订单同屏追踪（部分场景） | 推送通知状态更新 | 系统推送通知 + Widget | 桌面通知 | cron 定时查询 + 通知 |

### WK 协作办公类

#### WK1 对话/IM

**用户意图：** 与一个或多个对象进行实时文字/多媒体通讯。

**操作集合：** 发送文本/图片/文件/语音消息、撤回已发消息（时间窗口内）、引用回复特定消息、标记已读、搜索历史消息。

**数据状态：** 消息流（append-only，按时间排序）；未读计数；输入中状态（typing indicator）。

**信息流向：** WebSocket 实时推送新消息 → 追加到消息列表尾部；历史消息通过上拉触发 `GET messages(before_id, limit)` 反向加载。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 消息输入 | 底部输入框，Enter 发送，Shift+Enter 换行 | 底部输入框（随软键盘上推） | 底部输入框（系统键盘） | 底部输入框，Enter 或 Ctrl+Enter 发送 | 单行输入框，Enter 发送 |
| 历史加载 | 向上滚动触发加载更多 | 向上滚动触发加载更多 | 向上滚动（反向 InfiniteScroll） | 向上滚动触发 | `history` 命令分页查看 |
| 文件发送 | 拖拽文件到输入区 / 附件按钮 | 附件按钮（系统文件选择） | 附件按钮（相册/文件） | 拖拽 / 附件按钮 | `send --file path` |
| 消息操作 | 右键菜单（撤回/引用/复制） | 长按弹出操作菜单 | 长按弹出 Action Sheet | 右键菜单 | 选中消息 ID 后操作命令 |
| 平台特有 | 多窗口并排多个对话 | 推送通知（浏览器权限） | 系统推送 + 角标 | 系统通知 + 角标 | 音效提示 + ANSI 高亮新消息 |

---

#### WK2 频道/群组

**用户意图：** 在多人公开或私有空间内发布内容、浏览历史消息、管理成员和权限。

**操作集合：** 发送消息（同 WK1）、@成员、固定消息、查看成员列表、管理成员权限（踢出/设管理员）、创建话题/Thread。

**数据状态：** 频道消息流 + 成员列表（含权限状态）+ 固定消息列表。

**信息流向：** WebSocket 推送频道消息 → 渲染消息流；成员操作 `POST channel/kick(memberId)` / `PUT member/role` 写回。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 频道列表 | 左侧侧边栏频道树 | 底部 Tab 切换 / 侧滑抽屉 | 侧滑抽屉 / 底部 Tab | 左侧侧边栏 | `list channels` + `join <name>` |
| 成员面板 | 右侧固定成员列表面板 | 顶部成员数按钮 → 成员页 | 顶部按钮 → 成员页 | 右侧可折叠成员面板 | `members` 命令 |
| 权限操作 | 右键成员 → 权限菜单 | 长按成员头像 → 操作菜单 | 长按 → Action Sheet | 右键菜单 | `kick/promote <userId>` |
| 固定消息 | 顶部固定消息横幅 | 顶部横幅（可折叠） | 顶部横幅 | 顶部横幅 | `pinned` 命令查看 |
| 平台特有 | 多频道并排（宽屏场景） | 移动优化消息密度 | 系统推送 + 频道角标 | 系统通知集成 | IRC 风格交互 |

---

#### WK3 文档编辑

**用户意图：** 创建或修改结构化文档，支持多人实时协同编辑。

**操作集合：** 插入/删除/移动内容块、文本格式化操作、插入图片/表格/代码块/链接、查看版本历史、恢复历史版本、@协作者评论。

**数据状态：** 文档内容（有 dirty/saved/conflict 状态）；协作者在线光标位置；版本快照列表。

**信息流向：** OT（Operational Transformation）或 CRDT 算法实时合并多端操作 → 广播给所有在线协作者；定期自动保存快照 `POST doc/save(content)`。

> **与 MG8 配置页的区别：** WK3 支持多人实时协同、有块级编辑结构、有版本历史系统；MG8 是单人单实体的表单编辑，无协同无版本。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 编辑操作 | 键盘输入 + 鼠标格式化工具栏 | 触摸选择 + 浮动格式工具栏 | 触摸选择 + 键盘扩展工具栏 | 键盘 + 菜单栏工具 | Vim/Emacs 风格键盘编辑（如 `nano`/`vim`） |
| 协同光标 | 其他用户光标显示（彩色标注） | 其他用户光标显示（简化） | 其他用户光标显示 | 其他用户光标显示 | 无实时协同光标（TUI 限制） |
| 版本历史 | 时间线侧边栏，点击预览/恢复 | 底部 Sheet 版本列表 | 独立版本历史页 | 侧边版本面板 | `git log` 风格版本查看 |
| 块操作 | 拖拽块、`/` 命令插入新块 | 长按拖拽块（受限） | 长按拖拽块 | 拖拽块 | 行号操作 |
| 平台特有 | 最佳协同体验（全功能） | 基础编辑（复杂格式受限） | 适合移动场景简单文档 | 离线文档全功能 | 适合纯文本/代码文档 |

---

#### WK4 画布/白板

**用户意图：** 在自由坐标空间创作结构化或非结构化内容（如流程图、思维导图、设计稿）。

**操作集合：** 在画布上创建节点/图形/文本、绘制连线、拖拽移动元素、缩放/平移画布视口、批量框选元素、连接节点（边）。

**数据状态：** 画布元素集合（每个元素有坐标/尺寸/样式/内容状态）；视口状态（缩放比例/偏移）；undo/redo 操作历史栈。

**信息流向：** 元素操作 → 实时协同同步（CRDT）→ 广播给所有协作者；视口变化为纯本地状态，不同步。

> **与 WK3 文档编辑的核心区别：** WK4 是自由坐标系（绝对定位），元素可以重叠、任意放置；WK3 是文档流（块级顺序排列）。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 画布操作 | 鼠标拖拽 + 滚轮缩放 + 键盘辅助 | 触摸拖拽 + pinch 缩放 | 触摸拖拽 + pinch 缩放 + Apple Pencil | 鼠标拖拽 + 触控板手势 | N/A（自由坐标系无法在字符终端实现） |
| 元素创建 | 工具栏选择类型 + 画布拖拽绘制 | 触摸双击 / 工具栏 | 触摸双击 / Apple Pencil 绘制 | 工具栏 + 鼠标绘制 | N/A |
| 连线操作 | hover 节点边缘出现连接点，拖拽连线 | 长按节点 → 连线模式 | 长按节点 → 连线模式 | 同 Web 桌面 | N/A |
| 多人协同 | 实时同步 + 他人光标/框选可见 | 实时同步（简化光标显示） | 实时同步 | 实时同步 | N/A |
| 平台特有 | 键盘快捷键密集使用（设计师场景） | 触摸优化（适合简单白板） | Apple Pencil 自然书写 | 触控笔支持 | N/A（核心交互依赖自由坐标鼠标/触摸） |

---

#### WK5 看板（Kanban）

**用户意图：** 通过列来可视化工作状态，通过拖拽卡片在列间移动来改变任务状态。

**操作集合：** 创建任务卡片（指定初始列）、拖拽卡片到其他列（触发状态变更）、在卡片内编辑详情、归档/删除卡片、管理列定义。

**数据状态：** 列定义集合（列名/顺序）+ 各列卡片列表（每张卡片有状态/负责人/标签等）。

**信息流向：** 拖拽到新列 → `PUT card/move({cardId, targetColumnId})` 写回状态变更 → 实时同步给协作者。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 列展示 | 水平并排多列（横向滚动） | 单列展示 + 横向滑动切换列 | 单列展示 + 横向滑动 | 水平并排多列 | 列名列表 + `--column` 筛选 |
| 卡片拖拽 | 鼠标拖拽跨列 | 长按卡片 → 拖拽（受限） | 长按卡片 → 拖拽 | 鼠标拖拽 | `move <cardId> --to <column>` 命令 |
| 卡片详情 | 点击卡片 → 弹出侧边详情面板 | 点击卡片 → 全屏详情页 | 点击卡片 → push 详情页 | 点击卡片 → 弹出面板 | `view <cardId>` 命令 |
| 新建卡片 | 列底部「+ 添加」按钮 | 列底部「+」按钮 | 列底部「+」按钮 / FAB | 列底部「+」按钮 | `create --column <name>` |
| 平台特有 | 泳道视图（按负责人分组）| 触摸拖拽需长按激活 | Haptic 拖拽反馈 | 键盘快捷键移动卡片 | 脚本批量移动卡片 |

---

#### WK6 甘特图

**用户意图：** 在时间维度规划和追踪任务进度，管理任务间的依赖关系。

**操作集合：** 创建任务（含时间段）、拖拽调整任务开始/结束时间、拖拽扩展/缩短时间段、设置任务依赖关系（前置任务）、标记任务完成进度（百分比）、切换时间粒度（日/周/月）。

**数据状态：** 任务列表（含时间段、依赖关系、完成度）；时间轴视口（当前显示时间范围）。

**信息流向：** 时间段拖拽 → `PUT task/timeline({taskId, startDate, endDate})` 写回；依赖关系设置 → `POST task/dependency({taskId, dependsOnId})` 写回；依赖关系校验（循环依赖检测）在服务端执行。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 时间轴展示 | 横向时间轴 + 左侧任务列表（两轴交叉） | 垂直任务列表为主（时间轴简化） | 横向滚动时间轴（受屏幕宽度限制） | 横向时间轴（同 Web） | 字符甘特图（`ascii-gantt` 风格） |
| 拖拽调整 | 鼠标拖拽任务条左右边缘（调整时间）/ 整体拖拽（移动） | 不支持拖拽（改为手动填写日期） | 长按拖拽（受限，精度低） | 鼠标拖拽（同 Web） | `set-dates <taskId> --start X --end Y` |
| 依赖连线 | hover 任务条右端 → 拖出连线到依赖任务 | 不支持连线（改为下拉选择） | 长按 → 选择依赖关系 | 同 Web 桌面 | `depend <taskId> --on <depId>` |
| 时间粒度 | 工具栏切换（日/周/月/季度） | 仅支持周/月（空间受限） | 段控件切换 | 工具栏切换 | `--scale week` 参数 |
| 平台特有 | 关键路径高亮（可选） | 简化为进度追踪列表 | 适合查看不适合编辑 | 与 Outlook 日历集成（可选） | 适合 CI 流水线可视化 |

---

#### WK7 文件管理器

**用户意图：** 浏览、组织和操作文件/文件夹的层级结构。

**操作集合：** 进入/返回目录（导航）、上传文件、创建文件夹、移动文件/文件夹（拖拽或剪切粘贴）、重命名、删除（移入回收站）、下载、分享（生成链接）。

**数据状态：** 当前路径（面包屑导航状态）+ 当前目录内容列表（文件/文件夹）。

**信息流向：** `GET dir(path)` 加载目录内容 → 路径导航更新 → 写操作（`POST upload` / `POST mkdir` / `PUT move` / `DELETE`）改变目录状态 → 刷新当前目录。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 目录结构 | 左侧树 + 右侧内容区（双栏）或单栏列表/网格 | 单栏垂直列表（无左侧树） | Files App 风格（层级 push）/ 单栏 | Windows Explorer 双栏 | 字符树形目录（如 `ranger`、`mc`） |
| 文件操作 | 右键上下文菜单 | 长按弹出操作菜单 | 长按 → Action Sheet | 右键菜单 | 快捷键 + 命令 |
| 文件移动 | 拖拽（鼠标）/ Ctrl+X + Ctrl+V | 不支持拖拽，改为「移动到」菜单 | 长按拖拽（有限）/ 移动到选项 | 拖拽（鼠标） | `mv src dest` 命令 |
| 路径导航 | 顶部面包屑（可点击） | 顶部面包屑（横向滚动） | 导航栏（系统级 push/pop） | 地址栏 + 面包屑 | `pwd` + `cd` 命令 |
| 平台特有 | 键盘快捷键（Ctrl+C/V/X/Del） | 系统文件选择器集成 | 系统文件 App 深度集成 | 系统资源管理器集成 | **最自然平台**（ranger/mc 是典型 TUI 场景） |

### RT 通讯实时类

#### RT1 通话界面

**用户意图：** 与对方进行语音或视频实时通话。

**操作集合：** 静音/取消静音、开启/关闭摄像头（视频通话）、切换扬声器/听筒、挂断通话、切换至悬浮小窗/画中画、邀请更多参与者（可选）。

**数据状态：** 通话状态机（呼叫中/连接中/通话中/已挂断）；本地媒体状态（麦克风/摄像头开关）。

**信息流向：** WebRTC 信令握手（通过信令服务器）→ 点对点媒体流传输；媒体设备状态变更为纯本地，不经服务端。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 主操作区 | 屏幕中央/底部操作栏（静音/挂断/摄像头） | 底部固定操作栏 | 底部操作栏（系统级拨号界面风格） | 悬浮操作窗口 | N/A（媒体流无法在终端渲染） |
| 横竖屏 | 固定横向（宽屏） | 自动旋转支持 | 强制横屏（视频通话时） | 可调整窗口比例 | N/A |
| 小窗模式 | 浏览器画中画（PiP） | 浏览器 PiP | 系统级画中画（iOS PiP API） | 应用小窗置顶 | N/A |
| 多人通话 | 网格布局显示多人视频 | 纵向滚动视频列表 | 网格/焦点模式切换 | 网格布局 | N/A |
| 平台特有 | 屏幕共享（系统权限） | 屏幕共享（部分浏览器支持） | 系统级屏幕录制权限 | 屏幕共享（完整支持） | N/A（核心依赖音视频媒体流） |

---

#### RT2 直播间

**用户意图：** 观看实时视频内容，与主播和其他观众互动。

**操作集合：** 发弹幕/评论、点赞（持续点击）、送虚拟礼物、关注主播、举报、分享直播间链接。

**数据状态：** 直播状态（直播中/已结束）；弹幕流（实时 append-only）；在线人数；礼物动效队列。

**信息流向：** 视频流拉取（HLS/RTMP）→ 实时播放；WebSocket 推送弹幕/礼物事件 → 追加到弹幕层；用户互动操作（礼物/点赞）→ 写回服务端 → 广播给其他观众。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 视频区 | 页面主体区域（16:9） | 全宽顶部区域（9:16 竖屏） | 全屏竖向（9:16） | 页面主体区域 | N/A（视频流无法在终端渲染） |
| 弹幕 | 视频上方滚动文字层 | 视频上方（密度自适应） | 视频上方（密度自适应） | 视频上方 | N/A |
| 互动区 | 右侧评论区 + 礼物面板 | 底部输入区 + 礼物按钮 | 底部输入区 + 礼物按钮 | 右侧互动区 | N/A |
| 礼物动效 | 全屏动画覆盖层（短暂） | 全屏动画（短暂） | 全屏动画 + Haptic | 全屏动画 | N/A |
| 平台特有 | 多直播间并排观看（可选） | 横竖屏自动切换 | 最佳体验（原生 9:16） | 大屏优势 | N/A（核心依赖视频流） |

---

#### RT3 邮件收发

**用户意图：** 接收、阅读、回复邮件，撰写并发送新邮件，管理邮件分类。

**操作集合：** 浏览邮件列表（含分文件夹/标签）、阅读邮件详情、回复/转发、撰写新邮件（含附件）、标记已读/未读、归档、删除、移动到文件夹、搜索邮件。

**数据状态：** 已读/未读；标签/文件夹归属；草稿（本地存储 + 服务端自动保存）。

**信息流向：** IMAP 协议拉取邮件列表/详情（或 REST API）→ 展示；SMTP 协议发送（或 REST API）；草稿 `PUT draft(content)` 定时自动保存。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 整体布局 | 三栏（文件夹列表 / 邮件列表 / 邮件详情） | 单栏（列表 → 详情，独立页） | 两栏（iPad）/ 单栏（iPhone）/ 系统邮件 App | 三栏（同 Web） | mutt/neomutt 风格三区域 |
| 撰写邮件 | 悬浮撰写窗口（不遮挡列表）| 全屏撰写页 | 全屏撰写页（系统键盘） | 独立撰写窗口 | 编辑器（vim/nano）+ 发送命令 |
| 附件处理 | 拖拽文件到撰写区 | 附件按钮 → 系统文件选择 | 附件按钮 → 系统文件/相册 | 拖拽 + 附件按钮 | `attach file` 命令 |
| 搜索 | 顶部搜索框（实时搜索） | 顶部搜索框 | 顶部 UISearchController | 顶部搜索框 | `search "query"` 命令 |
| 平台特有 | 多账户多标签页 | 推送通知（浏览器权限） | 系统邮件 App 深度集成 | Outlook 插件生态 | 适合批处理/自动化邮件 |

---

#### RT4 通知中心

**用户意图：** 查看来自系统或业务的各类通知，处理待办提醒，快速跳转到相关内容。

**操作集合：** 标记单条通知已读、批量标记全部已读、删除/清除通知、点击通知跳转到相关实体页面。

**数据状态：** 通知列表（每条有已读/未读状态、类型、时间）；未读通知总计数（通常在导航处显示角标）。

**信息流向：** `GET notifications(page)` 加载历史通知 → WebSocket 推送新通知实时追加到列表顶部 → 已读操作 `PUT notification/read(id)` / `PUT notifications/read-all` 写回。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 入口 | 导航栏铃铛图标 + 未读角标 | 导航栏铃铛 + 角标 | 导航栏铃铛 / 系统通知中心 | 系统托盘 + 应用内图标 | `notifications` 命令 / `--watch` 监听 |
| 列表展示 | 下拉面板 / 独立页（通知多时） | 独立全屏页 | 独立页 / 系统通知中心 | 下拉面板 / 独立页 | 格式化文本列表（按时间分组） |
| 侧滑操作 | hover 显示操作按钮 | 侧滑删除/已读 | 侧滑操作（UISwipeAction） | hover 显示操作按钮 | 选中后命令操作 |
| 跳转目标 | 点击通知跳转到相关实体页 | 同上 | 同上（可直接从系统通知跳入） | 同上 | 输出跳转 URL / ID |
| 平台特有 | 多类型通知分组显示 | 浏览器推送通知（权限） | 系统推送 + 角标 + 通知中心 | 系统通知 + 任务栏提示 | 集成 cron / webhook 触发 |

---

### SB 审核提交类

#### SB1 审核型提交

**用户意图：** 将自己创建的内容或实体提交给平台审核，等待审核通过后方才生效上线。

**操作集合：**
- 创建草稿（可保存不提交，随时继续编辑）
- 提交审核（进入待审队列，不可再编辑）
- 查看当前审核状态（待审/审核中/已通过/已驳回）
- 驳回后：查看驳回原因 → 修改内容 → 重新提交
- 撤回已提交申请（回到草稿状态，可再次编辑）

**数据状态：**
`草稿 → 待审核 → 审核中 → 已通过 / 已驳回 → (修改后) → 待审核`

**信息流向：** 提交操作 `POST submit(entityId)` → 写入 MG4 审批队列；审核结果通过推送/轮询通知提交方；驳回后实体状态回到可编辑；通过后实体生效上线。

**与 MG4 的关系：** SB1（提交方视角）和 MG4（审核方视角）是**同一业务工作流的两端**。同一产品在 screen-map 中会出现两种标注——提交方界面标 SB1，审核方界面标 MG4。

**典型场景：** 商品上架、内容发布、广告投放、商家入驻申请、提现申请、KYC 实名认证。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 草稿编辑 | 独立编辑页（含字段表单） | 全屏编辑页 | push 编辑页 | 独立编辑窗口 | 交互式表单（TU2） |
| 提交操作 | 「提交审核」按钮（与「保存草稿」区分） | 底部「提交审核」按钮 | 导航栏「提交」按钮 | 工具栏按钮 | `submit <entityId>` |
| 审核状态 | 状态标签 + 驳回原因展示区 | 状态卡片 | 状态卡片 | 状态面板 | 文本状态 + 驳回原因 |
| 驳回后修改 | 编辑页重新打开（字段可编辑）+ 驳回原因提示 | 同上 | 同上 | 同上 | `edit <entityId>` 后重提交 |
| 平台特有 | 草稿自动保存（防止页面刷新丢失） | 移动端填写建议分步骤 | 推送通知审核结果 | 离线草稿支持 | 适合批量申请脚本化 |

---

### SY 引导系统类

#### SY1 Onboarding/引导流程

**用户意图：** 了解产品核心价值和关键功能，完成首次必要配置，授权必要权限，顺利开始使用产品。

**操作集合：** 浏览/跳过引导步骤、填写初始配置信息（如用户名/偏好）、授权系统权限（推送/位置等）、点击完成进入主界面。

**数据状态：** 引导完成状态（未开始/步骤 N/已完成）；写入服务端以防重复展示。

**信息流向：** 单向线性流程，各步骤数据本地暂存 → 全部完成后一次性 `POST onboarding/complete(data)` 写入；权限授权通过系统 API 触发。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 展示形式 | 全屏覆盖层或独立引导页 | 全屏页面序列 | 全屏页面序列（UIPageViewController） | 独立安装向导窗口 | 首次运行提示序列 |
| 步骤导航 | 底部圆点指示 + 下一步按钮 | 底部圆点 + 下一步 | 底部圆点 + 下一步（+ 跳过） | 上一步/下一步/完成 | 数字步骤提示 `[1/4]` |
| 权限授权 | 浏览器权限弹窗（由步骤触发） | 浏览器权限弹窗 | 系统权限弹窗（iOS/Android 原生） | Windows 权限对话框 | 无系统权限（TUI 受限） |
| 跳过选项 | 「稍后设置」链接 | 「跳过」链接 | 「跳过」按钮 | 「稍后」链接 | `skip` 命令 |
| 平台特有 | 可嵌入视频演示 | 轻量动画引导 | 系统 Haptic 反馈 | 离线安装配置 | 适合命令行工具配置初始化 |

---

#### SY2 向导/多步表单

**用户意图：** 通过分步骤完成一个复杂的创建或配置任务，每步独立聚焦，降低认知负担。

**操作集合：** 填写当前步骤表单字段、前进到下一步（带步骤内校验）、返回上一步（保留已填内容）、暂存草稿（可中途离开再继续）、最终提交。

**数据状态：** 当前步骤索引；各步骤完成/未完成状态；整体草稿保存状态（本地 + 可选服务端暂存）。

**信息流向：** 各步骤数据本地暂存 → 步骤校验通过后前进 → 最终步骤提交 `POST create(allStepData)` 或按步骤逐步保存（复杂场景）。

**平台矩阵：**

| 维度 | Web 桌面 | Mobile Web | iOS/Android | Windows | TUI |
|------|---------|------------|-------------|---------|-----|
| 步骤指示 | 顶部步骤条（水平，显示步骤名） | 顶部步骤条（简化，仅数字） | 顶部步骤条 / 导航栏标题「步骤 N/M」 | 左侧步骤列表 | `[步骤 N/M]` 文本标题 |
| 步骤切换 | 底部「上一步」/「下一步」按钮 | 底部固定「下一步」按钮 | 底部固定「下一步」按钮 | 底部「上一步」/「下一步」 | `next` / `back` 命令 |
| 表单校验 | 点击「下一步」时当前步骤全量校验 | 同上 | 同上 | 同上 | 提交步骤时校验，显示错误 |
| 草稿保存 | 自动保存（`beforeunload` 警告） | 自动保存 | 自动保存（`viewDidDisappear`） | 自动保存 | `save` 命令显式保存 |
| 平台特有 | 步骤跳转（点击已完成步骤直接跳） | 线性强制（不允许跳步）| 线性强制 | 步骤跳转（可选） | 适合 CLI 安装脚本配置 |

---

### TU TUI/CLI 类

> TU 类型本身就是平台，其「平台矩阵」维度改为「Shell 环境」差异。

#### TU1 命令行交互

**用户意图：** 通过文本命令执行操作，获取结构化文本输出，支持脚本化和自动化。

**操作集合：** 输入命令（含子命令/参数/flags）、查看输出（stdout/stderr）、Ctrl+C 中断、命令历史回溯（上箭头）、Tab 补全（Shell 提供）。

**数据状态：** 无持久 UI 状态，状态通过输出内容表达。

**信息流向：** stdin 命令 → 程序处理 → stdout 结构化输出 + 非零退出码表示错误。

**Shell 环境矩阵：**

| 维度 | Linux/macOS (bash/zsh) | Windows Terminal (PowerShell) | 嵌入式终端 (VS Code) | SSH 远程终端 |
|------|----------------------|------------------------------|---------------------|------------|
| 颜色输出 | ANSI 颜色（完整支持） | ANSI 颜色（部分支持，需配置） | ANSI 颜色（完整支持） | ANSI 颜色（取决于客户端） |
| Tab 补全 | bash-completion / zsh 补全 | PSReadLine 补全 | Shell 本身的补全 | 同远程 Shell |
| 管道支持 | 完整（`\|` `>` `<`） | 部分支持（语法差异） | 完整（同宿主 Shell） | 完整 |
| 交互输入 | `read` / `stty` | `Read-Host` | 同宿主 Shell | 同远程 Shell（含延迟） |
| 特殊考虑 | 最佳兼容基准 | Windows 路径分隔符差异 | 集成调试工具 | 网络延迟影响实时输出 |

---

#### TU2 交互式菜单（TUI）

**用户意图：** 通过键盘导航在文本界面中选择操作项，无需记忆命令名称。

**操作集合：** 方向键移动光标、回车确认选择、Esc 返回上一级、Space 切换多选项、`/` 搜索/筛选选项。

**数据状态：** 当前光标位置；已选择项集合（多选场景）。

**信息流向：** 键盘事件 → 光标移动/选择 → 确认后触发对应操作 → 输出结果 / 进入下一层菜单。

**Shell 环境矩阵：**

| 维度 | Linux/macOS | Windows Terminal | 嵌入式终端 | SSH 远程 |
|------|------------|-----------------|-----------|---------|
| 键盘映射 | 标准方向键 + 功能键完整 | 同上 | 同上（需终端支持） | 同上（某些键可能被 SSH 客户端截获） |
| 颜色/反白选中 | ANSI 背景色高亮 | 同上 | 同上 | 同上 |
| 鼠标支持 | 可选支持（ncurses） | 可选支持 | 可选支持 | 通常禁用（延迟问题） |
| 典型库 | ncurses / tview / bubbletea | 同上 | 同上 | 同上 |
| 特殊考虑 | 最成熟生态 | PowerShell 函数菜单（Out-GridView） | 终端尺寸自适应 | 低延迟优先，避免复杂 TUI |

---

#### TU3 日志流/监控

**用户意图：** 实时查看系统运行产生的日志输出，排查问题或监控状态。

**操作集合：** 关键词过滤（实时）、暂停/继续滚动（空格键）、保存输出到文件（重定向）、Ctrl+C 停止监听。

**数据状态：** 日志流（append-only，时间戳排序）；当前过滤条件；是否暂停滚动。

**信息流向：** 进程 stdout/stderr 实时 append → 过滤器处理 → 渲染到终端（自动滚动到最新）。

**Shell 环境矩阵：**

| 维度 | Linux/macOS | Windows Terminal | 嵌入式终端 | SSH 远程 |
|------|------------|-----------------|-----------|---------|
| 流式输出 | `tail -f` / `journalctl -f` / 自定义 | `Get-Content -Wait` / 自定义 | 同宿主 Shell | 同远程系统（带网络延迟） |
| 实时过滤 | `\| grep --line-buffered` | `\| Select-String` | 同宿主 Shell | 同上 |
| 颜色高亮 | ANSI 颜色（ERROR 红/WARN 黄） | 同上 | 同上 | 同上 |
| 暂停滚动 | 终端自身 Ctrl+S/Q 或 Space（自定义） | 同上 | 同上 | 同上（延迟敏感） |
| 特殊考虑 | 行缓冲 vs 块缓冲（unbuffer 命令） | 颜色支持需版本 >= Win10 | 嵌入限制终端行数 | 断连重连需保持会话（tmux） |

---

#### TU4 进度/任务流

**用户意图：** 跟踪多步骤任务的执行进展，了解当前步骤和整体完成情况。

**操作集合：** 查看（只读为主），可选 Ctrl+C 中断任务。

**数据状态：** 各步骤状态（等待中/进行中/已完成/已失败）；当前活跃步骤；整体进度百分比（可选）。

**信息流向：** 任务执行引擎 → 进度事件（步骤开始/完成/失败）→ 渲染更新（原地刷新或追加输出）。

**Shell 环境矩阵：**

| 维度 | Linux/macOS | Windows Terminal | 嵌入式终端 | SSH 远程 |
|------|------------|-----------------|-----------|---------|
| 进度条渲染 | ANSI 转义原地刷新（`\r`）/ 字符进度条 | 同上（Win10+ 支持 ANSI） | 同上 | 同上（延迟影响刷新频率） |
| 步骤状态图标 | Unicode 符号（✓ ✗ ⠋）+ ANSI 颜色 | 同上（需字体支持） | 同上 | 同上 |
| 并行任务 | 多行同时刷新（各行独立进度） | 同上 | 同上 | 延迟高时建议只输出关键事件 |
| 失败处理 | 非零退出码 + 红色错误信息 + 可选 --continue | 同上 | 同上 | 同上 |
| 特殊考虑 | 典型工具：`tqdm`/`progress`/`ora` | 同上 | CI 系统折叠步骤输出 | tmux 保持会话防断连 |

---

## 第三轴：上下文预设

> 上下文预设描述不同产品类型和用户属性下，各交互类型的出现频率和风格倾向。
> 频率标注：**高频**（该端主要界面）/ **中频**（常见但非主体）/ **低频**（偶发或少量）

### 产品类型预设

#### 电商平台

| 端 | 用户属性 | 高频类型 | 中频类型 | 低频类型 |
|---|---------|---------|---------|---------|
| 消费者 App | C端消费者 | CT1(Feed流) EC1(商品详情) | CT7(搜索结果) EC2(购物车) EC3(订单追踪) | CT3(Profile) MG1(订单列表) SY1(Onboarding) |
| 消费者 Web | C端消费者 | CT7(搜索结果) EC1(商品详情) | CT1(Feed流) EC2(购物车) | MG1(订单) CT3(Profile) |
| 商家后台 | B端商户 | MG2(CRUD集群) SB1(审核提交) | MG3(状态机) SY2(向导) MG1(只读列表) | MG4(审批) MG7(仪表盘) MG8(配置) |
| 运营后台 | 内部办公 | MG4(审批) MG3(状态机) | MG1(只读列表) MG7(仪表盘) | MG2(CRUD) MG6(树形) |

**典型端构成：** 消费者 App + 消费者 Web + 商家后台 + 运营后台
**导航结构：** 消费者端底部 Tab（首页/分类/购物车/我的）；后台侧边栏导航

---

#### 社交平台

| 端 | 用户属性 | 高频类型 | 中频类型 | 低频类型 |
|---|---------|---------|---------|---------|
| 用户 App | C端消费者 | CT1(Feed流) CT3(Profile) | CT2(内容阅读) CT8(Story) WK1(IM私信) | EC1(商品) RT4(通知) SY1(Onboarding) |
| 用户 Web | C端消费者 | CT1(Feed流) CT3(Profile) | CT2(内容阅读) CT7(搜索) | WK1(IM) RT4(通知) |
| 创作者后台 | B端商户 | MG2(内容CRUD) SB1(内容发布审核) | MG7(数据仪表盘) MG1(粉丝列表) | MG4(审批) MG8(配置) |
| 运营后台 | 内部办公 | MG4(内容审核) MG1(用户列表) | MG3(状态机) MG7(仪表盘) | MG2(CRUD) |

**典型端构成：** 用户 App + 用户 Web + 创作者后台 + 运营后台
**导航结构：** 用户端底部 Tab（首页/发现/发布/消息/我）

---

#### IM（即时通讯）

| 端 | 用户属性 | 高频类型 | 中频类型 | 低频类型 |
|---|---------|---------|---------|---------|
| 用户 App | C端消费者 | WK1(对话/IM) WK2(频道/群组) | CT3(联系人Profile) RT4(通知) | CT5(媒体播放) WK7(文件) RT1(通话) |
| 用户 Web | C端消费者 | WK1(对话/IM) WK2(频道/群组) | CT3(Profile) WK7(文件) | RT1(通话) MG8(设置) |
| 用户 Windows | C端消费者 | WK1(对话/IM) WK2(频道/群组) | WK7(文件) WK3(文档) | RT1(通话) MG8(设置) |
| 管理后台 | 内部办公 | MG1(用户列表) MG3(状态机) | MG7(仪表盘) MG4(举报审核) | MG2(CRUD) |

**典型端构成：** App + Web + Windows 客户端 + 管理后台
**导航结构：** 客户端左侧对话列表 + 频道树

---

#### 社区平台

| 端 | 用户属性 | 高频类型 | 中频类型 | 低频类型 |
|---|---------|---------|---------|---------|
| 用户 App/Web | C端消费者 | CT1(Feed流) CT2(内容阅读) | CT7(搜索) CT3(Profile) | WK1(私信) RT4(通知) CT4(卡片探索) |
| 版主后台 | B端商户 | MG4(内容审核) MG1(帖子列表) | MG3(状态机/封禁) MG2(CRUD) | MG7(版块统计) |
| 运营后台 | 内部办公 | MG4(审核) MG1(用户/内容列表) | MG7(仪表盘) MG3(状态机) | MG2(CRUD) MG6(分类树) |

**典型端构成：** 用户端（App+Web）+ 版主工具 + 运营后台

---

#### 工具类产品

| 端 | 用户属性 | 高频类型 | 中频类型 | 低频类型 |
|---|---------|---------|---------|---------|
| 用户 Web/App | 混合（C端+B端） | MG2(CRUD) MG8(配置) SY2(向导) | MG1(只读列表) MG3(状态机) | MG7(仪表盘) RT4(通知) SY1(Onboarding) |
| CLI 工具 | B端/内部 | TU1(命令行) TU4(进度) | TU2(交互菜单) TU3(日志流) | — |
| 管理后台 | 内部办公 | MG2(CRUD) MG1(只读列表) | MG7(仪表盘) MG8(配置) | MG4(审批) |

**典型端构成：** Web/App + CLI（可选）+ 管理后台

---

#### 办公类产品

| 端 | 用户属性 | 高频类型 | 中频类型 | 低频类型 |
|---|---------|---------|---------|---------|
| 用户 Web | B端商户/内部 | WK3(文档编辑) WK5(看板) WK1(IM) | WK6(甘特图) WK4(白板) WK7(文件) | RT3(邮件) MG8(设置) SY2(向导) |
| 用户 App | B端商户/内部 | WK1(IM) WK5(看板) | WK3(文档/移动) RT4(通知) | WK6(甘特图/查看) |
| 管理后台 | 内部办公 | MG2(CRUD) MG1(只读列表) MG3(状态机) | MG7(仪表盘) MG8(配置) MG4(审批) | MG6(组织架构树) |

**典型端构成：** Web + App + 管理后台（企业版）

---

#### 金融平台

| 端 | 用户属性 | 高频类型 | 中频类型 | 低频类型 |
|---|---------|---------|---------|---------|
| 用户 App | C端消费者 | MG7(仪表盘/资产) MG1(交易记录) | SY2(转账向导) SB1(开户/认证) MG8(账户设置) | MG5(账户详情) EC3(转账追踪) |
| 用户 Web | C端消费者 | MG7(仪表盘) MG1(交易记录) | SY2(向导) MG8(配置) | — |
| 风控后台 | 内部办公 | MG1(只读列表) MG4(审批/KYC) | MG7(仪表盘) MG3(状态机/冻结) | MG2(CRUD) |
| 运营后台 | 内部办公 | MG4(审批) MG1(用户/交易列表) | MG7(仪表盘) MG3(状态机) | MG2(CRUD) |

**典型端构成：** App + Web + 风控后台 + 运营后台

---

#### 内容/媒体平台

| 端 | 用户属性 | 高频类型 | 中频类型 | 低频类型 |
|---|---------|---------|---------|---------|
| 用户 App | C端消费者 | CT5(媒体播放) CT1(Feed流) CT8(短视频) | CT2(内容阅读) CT6(相册) CT7(搜索) | CT3(Profile) WK1(评论/IM) |
| 用户 Web | C端消费者 | CT5(媒体播放) CT7(搜索) CT2(阅读) | CT1(Feed) CT6(相册) | CT3(Profile) |
| 创作者后台 | B端商户 | MG2(内容CRUD) SB1(内容发布) | MG7(数据仪表盘) MG1(评论管理) | MG4(审核) |
| 运营后台 | 内部办公 | MG4(内容审核) MG1(内容列表) | MG7(仪表盘) MG3(状态机) | MG2(CRUD) |

**典型端构成：** App + Web + 创作者后台 + 运营后台

---

### 用户属性预设

#### C端消费者

- **信息密度：** 低（大图、大字号、充足留白）
- **错误容忍：** 高（友好提示，自动重试，避免技术词汇）
- **操作频率：** 低频偶发（每次操作间隔长，场景碎片化）
- **学习成本接受度：** 极低（无需培训即可使用）
- **风格倾向：** 视觉引导为主，减少文字说明，动画增强感知

#### B端商户/企业用户

- **信息密度：** 中（数据可见但不拥挤，关键数据突出）
- **错误容忍：** 中（有明确错误提示，允许重试，需要明确反馈）
- **操作频率：** 中频（每日固定使用，重复执行固定操作）
- **学习成本接受度：** 中（可接受简短引导和帮助文档）
- **风格倾向：** 效率与可读性平衡，允许少量专业词汇

#### 内部办公用户

- **信息密度：** 高（数据表格密集，多列多行，尽量减少翻页）
- **错误容忍：** 低（需要精确错误信息，允许技术细节可见）
- **操作频率：** 高频重复（每日大量操作，批量处理，快捷键需求高）
- **学习成本接受度：** 高（可接受系统培训，有使用手册）
- **风格倾向：** 功能完整性优先于视觉美观，键盘操作优先

---

## screen-map 推断规则

[推断规则内容 — Task 8]

---

## 跨端分布示例

[示例内容 — Task 9]

---

## 行为原语索引（Behavioral Primitives）

[行为原语内容 — Task 13]

---

## 各阶段使用指南

[使用指南内容 — Task 9]
