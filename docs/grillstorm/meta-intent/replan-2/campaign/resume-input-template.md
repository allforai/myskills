Resume the bootstrap you already started in this project from its saved state on disk. Do not restart it and do not re-interview me; everything already recorded stands.

Work only inside the project folder you are given.

Record the actual loaded entry and reference paths with their SHA-256 values, together with your host and independent session identity, in {{RECEIPT_PATH}}. Never invent session identity or successful tool results; report unavailable metadata explicitly. This is a second session on the same cell, so state plainly that it is a resume and which files you read in THIS session.

My next decision:

{{USER_TURN}}
