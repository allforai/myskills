## Anonymous Meta-Skill Feedback

Category: bug
Meta-skill version: 0.20.1

### Failure Pattern

evidence_freshness.py 第279行 consumed = expand_paths(...) 返回的是 _ReadEvaluation.expansions 中缓存的同一个 set，随后 consumed.update(...) 原地修改了缓存；同一评估内再次展开相同 input_dependencies 时 previous != expanded，抛出 'Input membership changed during freshness evaluation'，导致 validate_unattended_readiness 报 undetermined_external_change。触发条件：节点在 observed-input-dependencies 或 state extra 中有额外读取记录。项目本地修复：consumed = set(expand_paths(...))。

### Expected Behavior

The meta-skill should record or repair this class of workflow issue without exposing project-specific information.

### Suggested Fix

Update the relevant skill, bootstrap rule, validator, or orchestrator helper so future projects do not hit the same failure pattern.
