# Product Design Command For OpenCode

Source command:

- `../../../product-design-skill/commands/product-design.md`

OpenCode translation:

- Interpret `/product-design full`, `resume`, and similar forms as named
  workflow selections.
- Translate `allowed-tools`, `$ARGUMENTS`, and `AskUserQuestion` semantics into
  normal OpenCode behavior.
- Use `../execution-playbook.md` as the OpenCode-friendly orchestration contract.

