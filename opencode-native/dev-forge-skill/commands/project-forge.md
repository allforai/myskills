# Project Forge Command For OpenCode

Source command:

- `../../../dev-forge-skill/commands/project-forge.md`
- `../execution-playbook.md`

OpenCode translation:

- Treat `/project-forge full`, `existing`, and `resume` as workflow selectors.
- Use the source command as orchestration guidance, not as literal host syntax.
- Use `execution-playbook.md` as the default operating guide for phase routing,
  prerequisites, outputs, and final reporting.

