# Grok skill RED baselines

Captured before either Grok `SKILL.md` existed on 2026-07-18.

## Megastorm

Without the skill, the agent said it would likely replace mandatory Phase -1
with lightweight inspection, let one context implement and self-approve, edit a
dirty main worktree sequentially, rely too heavily on visible argv, and call a
class-wide goal complete from passing planned tasks without a census.

Observed rationalizations included:

- “The deadline makes a full environment probe disproportionate.”
- “Fresh context is less important because the same agent has the most
  repository knowledge.”
- “The user already launched Grok with these permissions, so inheriting them
  reflects intent.”
- “All planned tasks and tests passed, so the goal is complete.”

## Cross-exam

Without the skill, the agent preserved audit-only behavior and would not invent
evidence, but it would continue with author self-review when native subagents
were unavailable, optionally treat a headless process as a substitute reviewer,
and generate a scoped report rather than hard-stop before judgments.

This violates the required native-independent evidence gate even when the
result is labeled non-independent.

## GREEN expectations

- Megastorm requires Phase -1/0, census for class-wide claims, isolated writers,
  independent executor/supervisor contexts, and resolved effective security.
- Cross-exam hard-stops before judgments/report generation without fresh native
  subagents and never accepts a headless subprocess as a substitute.

