# Demo Forge Command For OpenCode

Source command:

- `../../../demo-forge-skill/commands/demo-forge.md`
- `../execution-playbook.md`

OpenCode translation:

- Treat `design`, `media`, `execute`, `verify`, `clean`, and `status` as named
  workflow branches.
- Translate Claude-only orchestration syntax into standard OpenCode behavior.
- Use `execution-playbook.md` as the default guide for runtime checks, quality
  gates, iterative repair, and final reporting.

