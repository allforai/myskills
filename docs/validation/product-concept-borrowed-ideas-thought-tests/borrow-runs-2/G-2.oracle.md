**Inherited decisions (baseline contract):**
- `runtime-smoke-verify` = 运行时契约验证，不是产品/指标验证。判据是"launch 成功 + 首个 API 的 2xx/预期 4xx + 首屏状态 + base URL/环境变量 readback"，且证据必须来自本节点**自己启动的 release artifact**。
- 台账硬约束：`verdict ∈ {done,gap,unprovable}`（不是数字）；entry 的 build 必须是 gate 时刻的整树身份；`mock_layers` 非空或响应撞 fixture 都不能判 done；任何 entry 被拒/文件缺失 ⇒ `overall` 不能 pass。
- 下游：`launch-prep` 读 `overall` 放行；`quality-checks` 读 `contract_drift_warnings[]`。

**Diagnosis（本节点对这份读数的判定）：**
- 这份读数里**没有一条是本节点的合法输入**：完成率/停留时长/券使用率/客服量都是产品漏斗与支持遥测，不是 runtime 烟测数据。本节点**不能**把 38.1% 折算成 done/gap/unprovable 之外的任何结论，更不能凭它发 `overall`。
- 读数本身指向的是**产品/可发现性回归，而非运行时契约故障**：券咨询 12→340、停留 42s→71s 是"找不到入口/来回找券"的形态；运行时契约 bug 的典型形态是崩溃/白屏/404，而不是停留变长。
- **归因不成立**：同一周还叠了"支付默认项改余额→上回用的"和另一团队 10% 灰度的"拼单"。周对周单臂对比、无对照，无法把 38.1% 归给券入口移动。

**Drift / contradiction check（要拦住的动作）：**
- 若编排器想用这些数字写 `smoke-report.json`（不管 overall=pass 还是 fail）——违反可采性契约：analytics 不是证据，build 也非 gate 时刻 artifact 身份，entry 不是本节点自己的 launch。
- 若把本节点当成"这次漏斗下滑的裁决者"——越权：这是 product-verify / 指标节点的职责。

**Recommendation（结论 + 留给编排器/后续节点的记录）：**
- 本节点判定：**out-of-scope / 不可采**。按契约记 `unprovable`（无本节点可验证的运行时证据面），**不发 overall**；若预上线 smoke 已跑且其 artifact identity 与当前树一致，则 smoke 结论仍独立成立（= 运行时全绿 ≠ 漏斗不掉，正是"全绿但用户看到坏体验"的另一侧盲区）。
- 记录（handoff）：
  1. 这份读数转交 **product/UX 验证**，不是 runtime-smoke；核心假设 = 券入口下移 + "自动最优券"开关覆盖了手动入口（咨询暴涨、停留变长支持此假设）。
  2. **要求带对照的实验**：把"券入口移动"与"支付默认项变更""拼单灰度"分离（A/B 或 switchback），当前数据不足以归因。
  3. 若真怀疑运行时/接口层面异常，本节点才补做：重跑 release artifact、取首屏截图 + 首个 API URL/状态 + base URL readback，并作为唯一合法证据。
- **不建议**用任何 worker 直接改结论；分析属于不同 capability。

**Risks:** ① 把 confounded 漏斗当成券改动因果，会误回滚/误修；② 用遥测冒充 smoke 证据会污染 `launch-prep` 的放行判据；③ 若 smoke 其实从未在预上线跑过，那是更严重的过程缺口，不是本次读数能补救的。

**Need from main agent:** 预上线 `runtime-smoke-verify` 到底跑没跑、其 `smoke-report.json`（含 entry/构建身份）在哪？这决定本案是"smoke 绿 + 产品回归"还是"烟测从未执行"。

**Suggested execution prompt:** 不warrant。这是判定/归因路由问题，不是实现交接；如上需要，只能由编排器补一次受控实验或确认既有 smoke entry。