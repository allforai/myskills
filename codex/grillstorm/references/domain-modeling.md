# Domain Modeling Adapter

Load official `domain-modeling` during official grilling. This is not a second protocol.

Persist official glossary and ADR updates in the repository layout from
`setup-matt-pocock-skills`. Copy durable excerpts into
`docs/grillstorm/<goal-slug>/decisions.md`.
