# Testforge Command For OpenCode

Source command:

- `../../../dev-forge-skill/commands/testforge.md`

OpenCode translation:

- Use the source command to drive testing workflow expectations.
- Preserve `.allforai/testforge/` outputs where generated.

