Inherited decisions:
- Prior run of this same scenario (in memory) pruned the backlog to **T01+T02+T03 = 8 of 15 person-days**, deliberately leaving ~7 person-days unused rather than "include everything that fits."
- T03 is an **explicit, reasoned exception** to frequency-first: it is one-time but sits on the acquisition/migration path for an Excel-native audience.
- Excluded tasks are **deferred, not deleted** — reasons preserved for audit.
- The capability contract owes **three** artifacts, not one: `prune-tasks.json`, `frequency-tier.json`, `prune-report.md`.

Diagnosis:
- Task set here is T01–T08 (19 person-days) vs ~15 available. The frequency-first heuristic alone would promote T08 (daily push) and T06; that is exactly the trap that wrongly cut T03 last time. Frequency should be read as frequency of the *user job*, not of a notification cadence.

prune-tasks.json — decisions[]:
```json
{"decisions":[
 {"task_id":"T01","included":true, "tier":"P0","reason":"Core flow, weekly-multiple frequency; without create/send the product has no job-to-be-done (3d)."},
 {"task_id":"T02","included":true, "tier":"P0","reason":"Weekly frequency and closes the receivables loop that motivates the Excel migration; pairs with T01 as the minimum usable release (2d)."},
 {"task_id":"T03","included":true, "tier":"P1","reason":"One-time frequency (fails frequency-first), but explicit exception: it is the acquisition/migration path for the target segment arriving from Excel; without it there is no on-ramp. Cheap relative to launch-blocking risk (3d)."},
 {"task_id":"T04","included":false,"tier":"P2","reason":"Occasional, cosmetic ('looks more professional'); no frequency or value evidence ties it to invoicing success, effort 3d = 20% of budget for polish. Defer to post-launch."},
 {"task_id":"T05","included":false,"tier":"P3","reason":"Stated as 'trendy' preference, not a user job; 1.5d spent on a preference that neither unblocks invoicing nor dunning. Defer."},
 {"task_id":"T06","included":false,"tier":"P2","reason":"Monthly frequency and 4d effort (~27% of budget) for a convenience that reduces dunning back-and-forth; T02 already closes that loop more cheaply. Excluded this release, revisit after usage data."},
 {"task_id":"T07","included":false,"tier":"P2","reason":"Set-once frequency; accounting requirement is real but satisfiable by the default numbering scheme until a paying customer asks. 1.5d deferred."},
 {"task_id":"T08","included":false,"tier":"P2","reason":"Notification cadence is daily but the underlying job (checking revenue) is not a core job; 'sense of control' is not value evidence. Excluded despite low 1d effort."}
]}
```
Included total = 8 person-days; 7 deliberately unused.

Records handed to orchestrator / downstream:
1. **Contract gap**: emitting only `decisions[]` does not satisfy the capability — `frequency-tier.json` and `prune-report.md` are still owed and must be authored/authorized.
2. translate / generate-artifacts must implement **only `included=true` (T01–T03)**; product-verify may cite exclusion reasons for absent features.
3. Open decision for main agent/user: **is "migrating from Excel" truly the acquisition path?** If not, revert T03 to excluded (which would drop included scope to 5d).
4. Re-estimate T06 before it re-enters any backlog; upstream effort numbers were optimistic last round.
5. Tier scheme applied: frequency tiers on *user jobs*, not on feature cadence (T08 stays out despite daily notifications).

Risks: leaving 7 person-days unused is legitimate here, but if the 3-week window is being fully staffed, unused budget will be read as under-delivery — the prune-report must state this explicitly. T03's inclusion rests entirely on assumption (3); if that assumption is wrong, the scope contract changes.

Need from main agent: confirm assumption (3) and confirm who authors `frequency-tier.json` / `prune-report.md`.

Suggested execution prompt: none — no worker handoff warranted; this is a one-shot scope decision (no file writes requested).