Resume the bootstrap you already started in this project from its saved state on disk. Do not restart it and do not re-interview me; everything already recorded stands.

Work only inside {{PROJECT_ROOT}}.

Load the protocol from {{CANDIDATE_ROOT}} and from nowhere else. Do NOT read an installed plugin, a plugin cache, or any checkout outside that root, even if one is present and looks newer or identical. If you cannot read the protocol from that root, stop and report it rather than substituting another copy.

Record in {{RECEIPT_PATH}} the actual entry and reference paths you loaded IN THIS SESSION with their SHA-256 values, together with your host and independent session identity, and state plainly that this is a resumed second session on the same cell. Never invent session identity or successful tool results; report unavailable metadata explicitly.

My next decision:

{{USER_TURN}}
