# Cross-Exam Judged Build Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A cross-exam completion report states which build it judged, recorded once at intake, so `/product-review` can check mechanically whether the report is stale instead of judging it from prose — and says "freshness unchecked" when it cannot.

**Architecture:** The evidence engine's `identity.py` gains a command-line entry so the examiner can compute the build string at intake and write it to a new run-level ledger field `judged_build`. Both renderers (the Claude one and the hand-forked Codex one; Pi reads the Codex tree) print it in the report header for `ledger_version >= 3`, and print "未记录" when it is absent. `/product-review` (two texts) compares the recorded commit with the repository's current state.

**Tech Stack:** Python 3 (stdlib + pytest), git, Markdown.

**Spec:** `docs/adr/0010-seams-are-guarded-from-the-receiving-side.md` — criterion 2 ("What crosses is checkable … a digest, a commit") and criterion 3 ("Unchecked is its own outcome"). The user decided on 2026-09-22 that the judged build is recorded in the ledger at intake, not computed at render time.

## Global Constraints

- Work directly on `main`, one commit per task. No worktree, no `git stash` / `checkout` / `reset` / `restore` / `clean`.
- Commit with an explicit pathspec (`git commit -F - -- <paths>`). Never `--no-verify`.
- End every commit message with exactly `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` — do not substitute another model name.
- No CI. Do not create anything under `.github/`. Do not push.
- `shared/evidence-engine/` is the source of the engine; its four mirrors are written only by `python3 shared/evidence-engine/sync.py` — never edit a mirror by hand.
- Do not bump `ledger_version`. New runs stay at 3; `pi/cross-exam/test_contract.py` pins the text `ledger_version: 3`.
- `claude/superstorm/scripts/fixtures/frozen-run.report.md` (and its Codex twin) must not change: the frozen run is `ledger_version: 2` and must render byte for byte as before.
- The ledger field is exactly: `"judged_build": {"build": "<string from identity.py>", "recorded_at": "<ISO 8601 with offset>", "excludes": ["<host dir>", ...]}`
- Header line when recorded, exactly: `被评构建：<build>（intake 记录于 <recorded_at>）`
- Header line when absent or malformed, exactly: `被评构建：未记录（intake 未写 judged_build；读这份报告的一方无法核对它是否过期）`

## Facts established on 2026-09-22

- Only author entries (from meta-skill `/run`) carry `build`. Prober entries do not, so the header cannot be derived from entries.
- `shared/evidence-engine/identity.py` exposes `build_identity(repo, artifacts=(), exclude=())` returning `{commit, snapshot, artifact, build, reason}`; `build` is `<commit>-<snapshot digest, 16 chars>[-<artifact digest, 16 chars>]`. The module has no `__main__`.
- Both renderers build the header with `out = [f"# 完成度报告 — {ledger['target']}", ""]` … `out.append(head)` followed by `out.append("")` (Claude: `render_report.py:625-635`; Codex: `:647-657`).
- Renderer tests import with `import render_report` / `from render_report import render` and build runs with `_mk_run(tmp, facets, entries)` and `_with_top(run, **keys)`. `_mk_run` writes no `ledger_version` (renders as version 1).
- `pi/cross-exam` resolves its protocol root to `codex/cross-exam-skill/`; it has no renderer or schema of its own.
- `docs/experience-review/runtime.md` already records `被评提交: <git sha | uncommitted | no-git>`.

---

### Task 1: The evidence engine can be asked for a build identity from the command line

**Files:**
- Modify: `shared/evidence-engine/identity.py` (append a `main` and the `__main__` guard; add `import json` and `import sys`)
- Test: `shared/evidence-engine/test_identity.py`
- Generated by sync (do not hand-edit): `claude/superstorm/knowledge/cross-exam/engine/identity.py`, `codex/cross-exam-skill/engine/identity.py`, `claude/meta-skill/scripts/engine/identity.py`, `codex/meta-skill/scripts/engine/identity.py`, and the same four `test_identity.py`

**Interfaces:**
- Consumes: the existing `build_identity(repo, artifacts=(), exclude=())`.
- Produces: `python3 <engine>/identity.py <repo> [--exclude DIR]...` prints one JSON object with keys `commit`, `snapshot`, `artifact`, `build`, `reason` and exits 0 when `build` is non-empty, 1 otherwise. Task 3 documents this exact command line.

- [ ] **Step 1: Write the failing tests**

Append to `shared/evidence-engine/test_identity.py`:

```python
def _cli(*args):
    import json
    import sys
    from pathlib import Path
    script = Path(__file__).with_name('identity.py')
    result = subprocess.run([sys.executable, '-B', str(script), *map(str, args)],
                            capture_output=True, text=True)
    return result.returncode, json.loads(result.stdout)


def test_command_line_prints_the_same_identity_the_function_returns(repo):
    code, printed = _cli(repo)
    assert code == 0, printed
    assert printed == build_identity(repo)
    assert printed['build'].startswith(printed['commit'] + '-')


def test_command_line_exclude_matches_the_function(repo):
    (repo / '.allforai').mkdir()
    (repo / '.allforai' / 'note.json').write_text('{}')
    code, printed = _cli(repo, '--exclude', '.allforai')
    assert code == 0, printed
    assert printed == build_identity(repo, exclude=['.allforai'])
    assert printed['build'] != build_identity(repo)['build']


def test_command_line_refusal_is_exit_one_with_a_reason(tmp_path):
    code, printed = _cli(tmp_path)
    assert code == 1
    assert printed['build'] == ''
    assert printed['reason']
```

- [ ] **Step 2: Run them and confirm they fail**

Run: `python3 -B -m pytest -q -p no:cacheprovider shared/evidence-engine/test_identity.py -k command_line`
Expected: 3 FAILED with `json.decoder.JSONDecodeError` (the script prints nothing today).

- [ ] **Step 3: Add the entry point**

In `shared/evidence-engine/identity.py`, change the import block

```python
import hashlib
import os
import subprocess
from pathlib import Path
```

to

```python
import hashlib
import json
import os
import subprocess
import sys
from pathlib import Path
```

and append at the end of the file:

```python


def main(argv):
    """`identity.py <repo> [--exclude DIR]...` prints build_identity as JSON; exit 1 when it has no build.
    For a caller that is not Python — an examiner recording which tree a run is about to judge."""
    args, exclude = list(argv[1:]), []
    while '--exclude' in args:
        at = args.index('--exclude')
        if at + 1 >= len(args):
            raise SystemExit('--exclude needs a directory')
        exclude.append(args[at + 1])
        del args[at:at + 2]
    if len(args) != 1:
        raise SystemExit('usage: identity.py <repo> [--exclude DIR]...')
    identity = build_identity(Path(args[0]), exclude=exclude)
    print(json.dumps(identity, ensure_ascii=False))
    return 0 if identity['build'] else 1


if __name__ == '__main__':
    sys.exit(main(sys.argv))
```

- [ ] **Step 4: Run the engine suite**

Run: `python3 -B -m pytest -q -p no:cacheprovider shared/evidence-engine`
Expected: 0 failed. (`test_mirrors.py` may fail here because the mirrors are now behind — if it does, that is the expected signal; go to Step 5 and rerun.)

- [ ] **Step 5: Sync the four mirrors and check them**

Run: `python3 shared/evidence-engine/sync.py && python3 shared/evidence-engine/sync.py --check; echo "check exit=$?"`
Expected: `check exit=0`.

Run: `python3 -B -m pytest -q -p no:cacheprovider shared/evidence-engine && for d in claude/superstorm/knowledge/cross-exam/engine codex/cross-exam-skill/engine claude/meta-skill/scripts/engine codex/meta-skill/scripts/engine; do python3 -B -m pytest -q -p no:cacheprovider $d | tail -1; done`
Expected: every line reports 0 failed.

- [ ] **Step 6: Commit**

```bash
git add shared/evidence-engine/identity.py shared/evidence-engine/test_identity.py claude/superstorm/knowledge/cross-exam/engine codex/cross-exam-skill/engine claude/meta-skill/scripts/engine codex/meta-skill/scripts/engine
git commit -F - -- shared/evidence-engine/identity.py shared/evidence-engine/test_identity.py claude/superstorm/knowledge/cross-exam/engine codex/cross-exam-skill/engine claude/meta-skill/scripts/engine codex/meta-skill/scripts/engine <<'EOF'
feat(evidence-engine): identity.py 可从命令行取构建标识

盘问官在 intake 时要记下这次评的是哪棵树，而它不是 Python 调用方。
`identity.py <repo> [--exclude DIR]...` 打印 build_identity 的 JSON，没有 build 时退出 1。
四个镜像由 sync.py 同步。

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 2: Both renderers print the judged build in the header

**Files:**
- Modify: `claude/superstorm/scripts/render_report.py` (new function above `def render(`; one insertion after `out.append(head)`)
- Modify: `codex/cross-exam-skill/scripts/render_report.py` (the same two edits)
- Test: `claude/superstorm/scripts/test_render_report.py`
- Test: `codex/cross-exam-skill/scripts/test_render_report.py`

**Interfaces:**
- Consumes: the ledger field `judged_build` exactly as defined in Global Constraints.
- Produces: for `ledger_version >= 3`, the line directly under the `需求基准：…` header line is one of the two exact header lines in Global Constraints. For lower versions the header is unchanged. Task 4 (`/product-review`) reads the text `被评构建：`.

- [ ] **Step 1: Write the failing tests (same code in both test files)**

Append to **both** `claude/superstorm/scripts/test_render_report.py` and `codex/cross-exam-skill/scripts/test_render_report.py`, directly above the final `if __name__ == "__main__":` line (if a file has none, append at the end):

```python
class TestJudgedBuild(unittest.TestCase):
    """The header names the tree the run judged, as recorded at intake; the renderer never recomputes it."""
    RECORDED = "被评构建：0123abc-89abcdef01234567（intake 记录于 2026-09-22T10:00:00+08:00）"
    MISSING = "被评构建：未记录（intake 未写 judged_build；读这份报告的一方无法核对它是否过期）"
    V3 = {"ledger_version": 3,
          "rule_consistency": {"status": "not_examined", "reason": "规则来源尚未审查"}}

    def _report(self, tmp, **top):
        run = _mk_run(tmp, [{"id": "F1", "name": "面一", "status": "examined"}],
                      [_entry("q1", verdict="gap", severity="low")])
        _with_top(run, **top)
        return render(run)

    def test_recorded_build_is_printed_under_the_baseline_line(self):
        with tempfile.TemporaryDirectory() as tmp:
            report = self._report(tmp, **self.V3, judged_build={
                "build": "0123abc-89abcdef01234567", "recorded_at": "2026-09-22T10:00:00+08:00",
                "excludes": [".allforai"]})
        lines = report.splitlines()
        at = next(i for i, line in enumerate(lines) if line.startswith("需求基准："))
        self.assertEqual(lines[at + 1], self.RECORDED)

    def test_a_v3_run_without_the_field_says_so(self):
        with tempfile.TemporaryDirectory() as tmp:
            self.assertIn(self.MISSING, self._report(tmp, **self.V3))

    def test_a_malformed_field_counts_as_not_recorded(self):
        for bad in ("0123abc", {"build": ""}, {"build": 7}, []):
            with tempfile.TemporaryDirectory() as tmp:
                self.assertIn(self.MISSING, self._report(tmp, **self.V3, judged_build=bad), bad)

    def test_older_ledgers_keep_their_header(self):
        with tempfile.TemporaryDirectory() as tmp:
            self.assertNotIn("被评构建", self._report(tmp, ledger_version=2))
        with tempfile.TemporaryDirectory() as tmp:
            self.assertNotIn("被评构建", self._report(tmp))
```

- [ ] **Step 2: Run them and confirm they fail**

Run: `python3 -B -m pytest -q -p no:cacheprovider claude/superstorm/scripts/test_render_report.py -k JudgedBuild` and the same for `codex/cross-exam-skill/scripts/test_render_report.py`.
Expected in each: 3 FAILED (`recorded`, `without_the_field`, `malformed`), 1 PASSED (`older_ledgers`). If a v3 run fails to render at all for a reason unrelated to the header (for example a rule-consistency refusal raising instead of rendering), stop and report the traceback rather than changing the fixture.

- [ ] **Step 3: Add the function and the header line to both renderers**

In **both** renderers, insert directly above the line `def render(run_dir):`:

```python
def _judged_build_line(ledger):
    """报告评的是哪棵树：intake 时记下的构建标识，原样打印。渲染器不重算——续盘后重渲染时，此刻的树可能已经
    不是被评的那棵；没记就说没记，读报告的一方据此知道新鲜度无从核对。"""
    recorded = ledger.get("judged_build")
    build = recorded.get("build") if isinstance(recorded, dict) else None
    if not isinstance(build, str) or not build.strip():
        return "被评构建：未记录（intake 未写 judged_build；读这份报告的一方无法核对它是否过期）"
    when = recorded.get("recorded_at")
    when = when.strip() if isinstance(when, str) and when.strip() else "时间未记录"
    return f"被评构建：{build.strip()}（intake 记录于 {when}）"


```

In **both** renderers, replace the single line

```python
    out.append(head)
```

with

```python
    out.append(head)
    if ledger.get("ledger_version", 1) >= 3:   # v2 and older render byte for byte as they always did
        out.append(_judged_build_line(ledger))
```

- [ ] **Step 4: Run both renderer suites, including the frozen golden**

Run: `python3 -B -m pytest -q -p no:cacheprovider claude/superstorm/scripts` then `python3 -B -m pytest -q -p no:cacheprovider codex/cross-exam-skill/scripts`
Expected: 0 failed in each, including `TestFrozenRunRendersByteIdentically`. If any other existing test compares a whole v3 report against a literal and now fails, that literal gains the MISSING line directly under its `需求基准：` line — report which test it was.

- [ ] **Step 5: Commit**

```bash
git commit -F - -- claude/superstorm/scripts/render_report.py claude/superstorm/scripts/test_render_report.py codex/cross-exam-skill/scripts/render_report.py codex/cross-exam-skill/scripts/test_render_report.py <<'EOF'
feat(cross-exam): 完成度报告表头写明被评构建，没记就写「未记录」

只有作者证据条目带 build，实测官条目没有，表头过去无从说明这份报告评的是哪棵树，
下游只能凭报告日期猜它过没过期。渲染器原样打印 intake 时记下的 judged_build，不重算；
ledger_version >= 3 才打印，v2 封存 run 逐字节不变。两份渲染器同改（Pi 走 Codex 这一份）。

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 3: Intake records the judged build

**Files:**
- Modify: `claude/superstorm/skills/cross-exam/SKILL.md` (定靶 step 6)
- Modify: `claude/superstorm/knowledge/cross-exam/schemas.md` (the `ledger.json` example and the bullet list under it)
- Modify: `codex/cross-exam-skill/SKILL.md` (定靶 step 6)
- Modify: `codex/cross-exam-skill/schemas.md` (the same two places)

**Interfaces:**
- Consumes: Task 1's command line; Task 2's header lines.
- Produces: skill text that makes a new run's ledger carry `judged_build`. No code.

- [ ] **Step 1: Claude SKILL.md, step 6**

Replace

```
6. 初始化/载入 `ledger.json`（schema 见 `$ROOT/knowledge/cross-exam/schemas.md`），新 run 写 `ledger_version: 3`，
   `rule_consistency: {status: "not_examined", reason: "规则来源尚未审查"}`。
```

with

```
6. 初始化/载入 `ledger.json`（schema 见 `$ROOT/knowledge/cross-exam/schemas.md`），新 run 写 `ledger_version: 3`，
   `rule_consistency: {status: "not_examined", reason: "规则来源尚未审查"}`，以及 `judged_build`——这次评的是哪棵树。
   新 run 在派出第一问之前跑一次 `python3 $ROOT/knowledge/cross-exam/engine/identity.py <被测仓库根> --exclude <宿主隐藏目录>...`
   （`.allforai` / `.claude` / `.codex` 里存在的那些，以及本 run 目录相对仓库根的路径），把输出的 `build` 连同带时区偏移的
   `recorded_at` 和用过的 `excludes` 写进 `judged_build`。续接旧 run 不重写它：报告说的是开评时的树。
   命令退出 1（不是 git 仓库、读不了）就不写这个键，把它的 `reason` 记进 `open_threads`——渲染器会在表头写「被评构建：未记录」，
   读报告的一方据此知道新鲜度无从核对。不要编一个值。
```

- [ ] **Step 2: Codex SKILL.md, step 6**

Replace

```
6. 使用 `ledger_store.py` 加锁并初始化/载入 `ledger.json`（schema 见 `$ROOT/schemas.md`），新 run 写 `ledger_version: 3`，
   `rule_consistency: {status: "not_examined", reason: "规则来源尚未审查"}`。
```

with

```
6. 使用 `ledger_store.py` 加锁并初始化/载入 `ledger.json`（schema 见 `$ROOT/schemas.md`），新 run 写 `ledger_version: 3`，
   `rule_consistency: {status: "not_examined", reason: "规则来源尚未审查"}`，以及 `judged_build`——这次评的是哪棵树。
   新 run 在派出第一问之前跑一次 `python3 $ROOT/engine/identity.py <被测仓库根> --exclude <宿主隐藏目录>...`
   （`.allforai` / `.claude` / `.codex` 里存在的那些，以及本 run 目录相对仓库根的路径），把输出的 `build` 连同带时区偏移的
   `recorded_at` 和用过的 `excludes` 写进 `judged_build`。续接旧 run 不重写它：报告说的是开评时的树。
   命令退出 1（不是 git 仓库、读不了）就不写这个键，把它的 `reason` 记进 `open_threads`——渲染器会在表头写「被评构建：未记录」，
   读报告的一方据此知道新鲜度无从核对。不要编一个值。
```

- [ ] **Step 3: Both schemas.md files**

In **both** `claude/superstorm/knowledge/cross-exam/schemas.md` and `codex/cross-exam-skill/schemas.md`, in the `ledger.json` example, replace the line

```
  "started": "YYYY-MM-DD",
```

with

```
  "started": "YYYY-MM-DD",
  "judged_build": {"build": "<commit>-<snapshot 摘要前 16 位>", "recorded_at": "2026-09-22T10:00:00+08:00",
                   "excludes": [".allforai", "docs/cross-exam/2026-09-22-demo"]},
```

and, in the same two files, directly above the line that begins `- **`ledger_version >= 2`**`, insert:

```
- **`judged_build`**（`ledger_version >= 3` 的 run）：intake 时由 `engine/identity.py` 算一次、原样记下的构建标识，
  是这份报告评的那棵树。渲染器只打印不重算（续盘重渲染时此刻的树可能已经变了），打印在表头「需求基准」下一行：
  `被评构建：<build>（intake 记录于 <recorded_at>）`；缺这个键或 `build` 不是非空字符串 → `被评构建：未记录（…）`，不拒渲。
  它和 entry 上的 `build` 不是一回事：那是作者证据各自取证时的树，由渲染器对此刻的树重算核对。
```

- [ ] **Step 4: Check the contract tests that read these files still pass**

Run: `python3 -B -m pytest -q -p no:cacheprovider claude/superstorm/scripts codex/cross-exam-skill/scripts pi/cross-exam shared/keep-code-simple`
Expected: 0 failed. (`pi/cross-exam/test_contract.py` pins `ledger_version: 3`; it must still find it.)

- [ ] **Step 5: Commit**

```bash
git commit -F - -- claude/superstorm/skills/cross-exam/SKILL.md claude/superstorm/knowledge/cross-exam/schemas.md codex/cross-exam-skill/SKILL.md codex/cross-exam-skill/schemas.md <<'EOF'
feat(cross-exam): intake 记下 judged_build——这次评的是哪棵树

派出第一问之前用 engine/identity.py 算一次构建标识写进台账；续接旧 run 不重写。
算不出来就不写、把原因记进 open_threads，表头会如实写「未记录」，不编值。
Claude 与 Codex 两份 SKILL、两份 schemas 同改；Pi 读 Codex 这一份。

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 4: /product-review checks freshness against the recorded build

**Files:**
- Modify: `claude/superstorm/skills/product-review/SKILL.md` (the two "Prior evidence" paragraphs)
- Modify: `codex/cross-exam-skill/product-review.md` (the same two paragraphs)

**Interfaces:**
- Consumes: the header text `被评构建：` from Task 2; the existing `被评提交:` line of `docs/experience-review/runtime.md`.
- Produces: skill text only.

- [ ] **Step 1: The cross-exam paragraph, in both files**

In **both** files, in the paragraph beginning `**Prior evidence.** If `docs/cross-exam/*/completion-report.md` exists`, replace the sentence

`产品在那份报告之后动过，也不在这里重走旅程补判：记下报告日期与你看到的差异，把该工作的前提标成过期，`

with

`过没过期要核，不凭印象：取表头「被评构建：」里的 commit（`-` 之前那段），跑 `git log --oneline <commit>..HEAD -- . ':!docs'` 和 `git status --short -- . ':!docs'`；有输出就是产品在那之后动过，也不在这里重走旅程补判：记下被评构建与这两条命令的输出摘要，把受影响工作的前提标成过期，`

and, in the same paragraph, directly before the sentence `Absent → write `Prior evidence: none`.`, insert:

`表头写「被评构建：未记录」、没有这一行（旧报告）、或这两条命令跑不了（不是 git 仓库、commit 不在本仓库）→ 在 `Prior evidence` 行写 `新鲜度未核：<原因>`，照常引用它的 `G` / `J` 号，但不把它当成对当前产品的观察，也不写成「未过期」。`

- [ ] **Step 2: The runtime-review paragraph, in both files**

In **both** files, in the paragraph beginning `**Prior evidence — runtime experience review.**`, replace the sentence

`过期纪律与 cross-exam 报告相同：产品在「被评提交」之后动过，就记下评审日期、被评提交与你看到的差异，把受影响的格标成过期，不在这里重判；`

with

`过期纪律与 cross-exam 报告相同，同样要核：用该文件的「被评提交」跑同样两条命令；有输出就记下评审日期、被评提交与输出摘要，把受影响的格标成过期，不在这里重判；「被评提交」是 `uncommitted` / `no-git` 或命令跑不了 → 写 `新鲜度未核：<原因>`，不写成未过期；`

- [ ] **Step 3: Confirm both files changed identically and nothing pins the old sentences**

Run: `git diff --stat -- claude/superstorm/skills/product-review/SKILL.md codex/cross-exam-skill/product-review.md` and `grep -c "新鲜度未核" claude/superstorm/skills/product-review/SKILL.md codex/cross-exam-skill/product-review.md`
Expected: both files changed; each count is `2`.

Run: `python3 -B -m pytest -q -p no:cacheprovider claude/superstorm/scripts codex/cross-exam-skill/scripts shared/keep-code-simple pi/cross-exam`
Expected: 0 failed.

- [ ] **Step 4: Thought-test the new text (one scenario, Sonnet subject)**

Dispatch one read-only subagent on the `sonnet` model with exactly this prompt:

> You are running `/product-review` per `/Users/aa/workspace/myskills/claude/superstorm/skills/product-review/SKILL.md`. Read only that file. Do not run the product. A target repository has `docs/cross-exam/2026-09-01-shop/completion-report.md` whose header's second line reads `被评构建：未记录（intake 未写 judged_build；读这份报告的一方无法核对它是否过期）`, and its gap list contains `G1 退款重复扣减`. The job in scope is "买家申请退款". Write only the `Prior evidence` line of your report and one sentence explaining it.

Pass when the returned line cites `G1`, contains `新鲜度未核`, and does not claim the report is current. If it fails, do not reword blindly: first check that the subject was asked for the artifact where the behaviour lands (the `Prior evidence` line), then adjust the Step 1 sentence and rerun once. Record the subject's output in the task report.

- [ ] **Step 5: Commit**

```bash
git commit -F - -- claude/superstorm/skills/product-review/SKILL.md codex/cross-exam-skill/product-review.md <<'EOF'
feat(product-review): 旧结论过没过期用被评构建去核，核不了就写「新鲜度未核」

过去只要求「记下报告日期与你看到的差异」，过期与否由模型凭印象判。现在用完成度报告表头的被评构建
（体验评审用它的被评提交）对 git log / git status 去核；表头写未记录、旧报告没有这一行、或命令跑不了，
一律写「新鲜度未核」并照常引用 G/J 号——核不了不是没过期（ADR-0010 第二、三条）。

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```
