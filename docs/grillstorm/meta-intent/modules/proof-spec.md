# Proof module

Spec revision 2. Sources #8 Testing Decisions and #15–#18 in ../sources. Owns evidence for O1/O2/O3 and SG-02 actual candidate loading/proof freshness, not product code or confirmation authority. Dependencies exactly as native tracker: #15<-9,10; #16<-11; #17<-12,13; #18<-14.

Each thought issue's required scenarios and acceptance remain authoritative in its source snapshot. An evaluator may read those criteria; subject workers MUST NOT. Each subject gets one scenario, raw synthetic fixture and current Skill, scripted user turns as the dialogue proceeds, with fresh actual Claude or Codex session per scenario. Fixture outputs live in isolated temporary directories, not installed skills or unrelated workspaces. No role-playing a second host. Preserve provider/version and non-secret dispatch identity, raw user inputs, actual dialogue/tool outcomes, generated artifacts, final state and decisive assertions. Session credentials/capabilities redacted.

Root Orca coordinator launches subjects when evaluator asks; evaluator worker must not circumvent nested-worker guard. Limit to three live workers including evaluators/subjects/reviewers. Shared physical resources get explicit mutex; fixture directories unique, no global .allforai output. The scripted-user oracle is held outside subject context. Unavailable environment is an explicit unverified scenario; no skipped scenario counts as passing.

Failures produce minimal reproduction and owning implementation ticket link; fix through its owner and fresh retest. Read-only review scopes do not authorize evaluator production edits. A thought ticket may add/maintain its scenario definitions and evidence index; production corrections reenter the implementation task. Each issue independent—no hidden shared-harness dependency on another thought issue. Shared fixture utilities only reuse existing tested helpers or require an explicitly revalidated producer edge. Global proof counts full required scenario denominator on both hosts; report-only evidence never closes functionality.
