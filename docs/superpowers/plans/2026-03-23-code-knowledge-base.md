# Code Knowledge Base Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add file-level knowledge base (file-catalog + code-index) to code-replicate Phase 2, and switch Phase 3 context loading from raw source to pre-digested cards with targeted fallback.

**Architecture:** Phase 2 Steps 2.3/2.3.5 are extended to generate a file card + quiz verification per file read. New Step 2.3.7 assembles cards into file-catalog.json (with per-module slices) and code-index.json. Phase 3 replaces "current module source code" with code-index + file-catalog slice. No new Python scripts — 100% LLM-driven.

**Tech Stack:** Markdown skill files (Chinese), JSON schemas

**Spec:** `docs/superpowers/specs/2026-03-23-code-knowledge-base-design.md`

---

### Task 1: Add file-catalog.json and code-index.json schemas to schema-reference.md

**Files:**
- Modify: `code-replicate-skill/docs/schema-reference.md` (append after test-vectors.json section, before fidelity-report.json section)

- [ ] **Step 1: Read the current schema-reference.md**

Already read. Insert new schemas between `test-vectors.json` section (ends line 401) and `fidelity-report.json` section (starts line 404).

- [ ] **Step 2: Add file-catalog.json schema**

Insert after the `---` on line 402:

```markdown
## file-catalog.json

File-level structured knowledge base produced by Phase 2 Step 2.3.7. Stored at `.allforai/code-replicate/file-catalog.json` (complete) and `.allforai/code-replicate/file-catalog-{module_id}.json` (per-module slices). Generated by LLM during Phase 2 Steps 2.3/2.3.5 (card generation), assembled in Step 2.3.7.

\`\`\`json
{
  "version": "1.0.0",
  "generated_at": "ISO8601",
  "files": [
    {
      "path": "internal/service/order_service.go",
      "module": "M002",
      "kind": "service",
      "symbols": [
        {
          "name": "CreateOrder",
          "type": "function",
          "signature": "(ctx, req) (*Order, error)",
          "business_intent": "Create order: validate inventory -> calculate price -> initiate payment"
        }
      ],
      "dependencies": ["M001/user_service.go", "M002/order_repo.go"],
      "business_summary": "Core order service handling create/cancel/query operations",
      "is_abstraction": false
    }
  ]
}
\`\`\`

**Field notes:**
- `files[]` — one card per source file, generated by LLM immediately after reading the file
- `path` — relative file path from source root
- `module` — module ID (M001, M002, ...) from source-summary. `null` for root-level config files (nginx.conf, routes.yaml) per iron law 18
- `kind` — LLM-judged file role. Free-form, no controlled vocabulary. Examples: controller, service, repository, model, middleware, util, config, test, view, component, hook, store, migration, script, proto. Phase 3 consumers MUST NOT branch logic on specific `kind` values
- `symbols[]` — public functions/classes/methods. Each has:
  - `name` — symbol name
  - `type` — function, class, method, interface, type, enum, constant
  - `signature` — parameter and return type signature
  - `business_intent` — one-sentence business description of what this symbol does
- `dependencies` — files this file imports/depends on (module/filename format)
- `business_summary` — one-sentence summary of the file's business purpose
- `is_abstraction` — true if this file is a shared abstraction consumed by multiple modules (per iron law 12). Default: false
- `abstraction_consumers` — module IDs consuming this abstraction. Only present when `is_abstraction: true`
- `confidence` — `high` (default) or `low` (quiz verification failed after max retries)
- Per-module slice files contain only the `files[]` entries for that module. File naming: `file-catalog-M001.json`, `file-catalog-root.json` (for module: null files)
```

- [ ] **Step 3: Add code-index.json schema**

Insert immediately after the file-catalog.json section:

```markdown
---

## code-index.json

Inverted indexes for cross-module querying, built from file-catalog cards. Stored at `.allforai/code-replicate/code-index.json`. Always loaded in Phase 3 context (iron law 26). Generated by LLM in Phase 2 Step 2.3.7.

\`\`\`json
{
  "version": "1.0.0",
  "generated_at": "ISO8601",
  "concepts": {
    "authentication": ["M001/auth_service.go", "M001/jwt_helper.go", "M003/auth_middleware.go"],
    "order_processing": ["M002/order_service.go", "M002/payment_service.go"]
  },
  "entities": {
    "User": {
      "definition": "M001/models/user.go",
      "fields": ["id", "email", "name", "role", "created_at"],
      "used_by": ["M001/user_service.go", "M002/order_service.go"]
    }
  },
  "api_surface": {
    "POST /api/orders": {
      "handler": "M002/order_controller.go:CreateHandler",
      "service": "M002/order_service.go:CreateOrder",
      "middleware": ["M003/auth_middleware.go"]
    }
  }
}
\`\`\`

**Field notes:**
- `concepts` — business concept keyword → file paths. LLM-generated, advisory — for navigation, not programmatic querying. Different runs may produce different concept keys
- `entities` — entity/model name → `{definition, fields, used_by}`. Query-optimized view; `source-summary.data_entities` remains the source of truth for entity schemas and relations
- `api_surface` — API endpoint → handler chain. Key format adapts to project type: `"POST /api/orders"` (HTTP), `"gRPC OrderService.CreateOrder"` (gRPC), `"WS /chat"` (WebSocket), `"GraphQL mutation createOrder"` (GraphQL), `"Event order.created"` (event-driven)
```

- [ ] **Step 4: Verify the edit**

Run: `grep -c "file-catalog.json\|code-index.json" code-replicate-skill/docs/schema-reference.md`
Expected: multiple matches confirming both schemas are present.

- [ ] **Step 5: Commit**

```bash
git add code-replicate-skill/docs/schema-reference.md
git commit -m "docs(code-replicate): add file-catalog and code-index schemas"
```

---

### Task 2: Extend stage-a-structure.md with card generation, quiz, and Step 2.3.7

**Files:**
- Modify: `code-replicate-skill/docs/phase2/stage-a-structure.md`

- [ ] **Step 1: Extend Step 2.3 with card generation instruction**

After the existing Step 2.3 content (line 37, before `## 2.3.5`), insert card generation guidance. Edit the end of Section 2.3 to add:

```markdown
**文件卡片生成**：读完每个 key_file 后，LLM 立即输出该文件的结构化卡片（JSON），包含 path、module、kind、symbols、dependencies、business_summary。卡片在读文件时注意力最集中的时刻生成，确保信息完整。

卡片生成后立即执行 **Quiz 验证**（自答 3 题）：
1. 这个文件的入口函数/导出接口是什么？
2. 它依赖哪些外部服务/模块？
3. 它处理的核心业务场景是什么？

答案与卡片不一致 → 重读文件 → 重新生成卡片。最多重试 2 次，仍不一致则标记 `"confidence": "low"` 继续。

**Quiz 示例**：
```
文件: order_service.go
卡片: symbols=[CreateOrder, GetOrder], dependencies=[user_service, order_repo]
Quiz: Q1=CreateOrder, CancelOrder, GetOrder  ← CancelOrder 不在卡片中！
      Q2=user_service, order_repo, payment_api ← payment_api 不在卡片中！
→ 不一致 → 重读 → 重新生成
```

模块摘要（responsibility、exposed_interfaces 等）在该模块所有文件卡片生成完毕后再总结，确保摘要与卡片一致。

source-summary.abstractions 中的文件，其卡片须标记 `is_abstraction: true` 和 `abstraction_consumers`（iron law 12）。

> 卡片 Schema 详见 ${CLAUDE_PLUGIN_ROOT}/docs/schema-reference.md#file-catalog
```

- [ ] **Step 2: Extend Step 2.3.5 with card generation for newly discovered files**

In the Step 2.3.5 section, after "**Step 3: 发现重要文件 → 补读**" (line 77), add:

```markdown
补读的文件也按 2.3 相同流程生成卡片 + Quiz 验证。
```

- [ ] **Step 3: Add Step 2.3.7 section**

After Step 2.3.5's "**底线**" paragraph (line 83) and before `## 2.4`, insert:

```markdown
## 2.3.7 知识库构建

Step 2.3 和 2.3.5 生成的所有文件卡片，在此步骤汇总为结构化知识库：

**a. 组装 file-catalog.json**
- 收集所有文件卡片 → 写入 `.allforai/code-replicate/file-catalog.json`
- 同时按模块拆分为切片文件：`file-catalog-M001.json`、`file-catalog-M002.json`、...
- 根级文件（`module: null`）写入 `file-catalog-root.json`

**b. 构建 code-index.json**
- 从所有卡片的 business_intent + business_summary 提取业务概念 → `concepts` 倒排索引
- 从 source-summary.data_entities 提取实体 + 从卡片补充 `used_by` → `entities` 索引
- 从卡片中 kind=controller/handler 的 symbols 提取 API 端点 → `api_surface` 索引
- 写入 `.allforai/code-replicate/code-index.json`

**c. 更新进度**
- 写入 replicate-config.progress step `"2.3.7"`
- file-catalog.json 原子写入 — 文件存在即代表 2.3.7 完成。崩溃后重启时，若 file-catalog.json 不存在则从 2.3 最后完成的模块继续

> Schema 详见 ${CLAUDE_PLUGIN_ROOT}/docs/schema-reference.md#file-catalog 和 #code-index
```

- [ ] **Step 4: Verify the edit**

Run: `grep -n "2.3.7\|Quiz\|file-catalog\|code-index" code-replicate-skill/docs/phase2/stage-a-structure.md`
Expected: Multiple matches across the new sections.

- [ ] **Step 5: Commit**

```bash
git add code-replicate-skill/docs/phase2/stage-a-structure.md
git commit -m "docs(code-replicate): extend Stage A with card generation, quiz, and Step 2.3.7"
```

---

### Task 3: Add Step 2.3.7 to Phase 2 Stage A table and iron law 26 in code-replicate-core.md

**Files:**
- Modify: `code-replicate-skill/skills/code-replicate-core.md`

- [ ] **Step 1: Add Step 2.3.7 row to Stage A table**

Find the Stage A table (lines 58-64). Add a new row after `2.3.5`:

Current table:
```
| 2.3.5 | 文件覆盖扫描 | 未读文件头部扫描 → 发现遗漏 → 补读 → coverage ≥ 50% |
| 2.4 | project_archetype | LLM 识别项目核心价值类型 |
```

Change to:
```
| 2.3.5 | 文件覆盖扫描 | 未读文件头部扫描 → 发现遗漏 → 补读 → coverage ≥ 50% |
| 2.3.7 | file-catalog + code-index | 汇总文件卡片 → 知识库（file-catalog.json + code-index.json） |
| 2.4 | project_archetype | LLM 识别项目核心价值类型 |
```

- [ ] **Step 2: Add iron law 26**

Find iron law 25 (line 279). After it, add:

```
26. **code-index 常驻上下文** — Phase 3 每次 LLM 调用必须同时加载 source-summary.json 和 code-index.json 作为全局视角。file-catalog 按模块切片加载（file-catalog-M001.json），替代直接读源码。需要精确实现细节时可定向回读源码中的具体函数
```

- [ ] **Step 3: Update progress step list comment**

Find the progress section (line 317). Update the Phase 2 steps range:

```
- Phase 2 的 15 步精确到 `"2.1"` ~ `"2.15"`
```

Change to:

```
- Phase 2 的步骤精确到 `"2.1"` ~ `"2.15"`（含 `"2.3.7"`）
```

- [ ] **Step 4: Verify the edits**

Run: `grep -n "2.3.7\|^26\.\|code-index 常驻" code-replicate-skill/skills/code-replicate-core.md`
Expected: Matches for all three additions.

- [ ] **Step 5: Commit**

```bash
git add code-replicate-skill/skills/code-replicate-core.md
git commit -m "docs(code-replicate): add Step 2.3.7 to Stage A table and iron law 26"
```

---

### Task 4: Update Phase 3 context loading in standard-artifact-steps.md

**Files:**
- Modify: `code-replicate-skill/docs/phase3/standard-artifact-steps.md`

- [ ] **Step 1: Replace the context loading section**

Find the context loading block (lines 115-119):

```markdown
**每次 LLM 调用的上下文**：
- source-summary.json（~4-8 KB，常驻全局视角）
- 当前模块源码（~10-30 KB）
- 目标 schema 定义（~2-4 KB，按需加载）
- replicate-config 摘要（~1 KB）
```

Replace with:

```markdown
**每次 LLM 调用的上下文**：
- source-summary.json（~4-8 KB，常驻全局视角）
- code-index.json（~5-10 KB，常驻全局视角 — iron law 26）
- file-catalog 模块切片（~3-8 KB，按 extraction-plan 模块引用加载 file-catalog-M001.json）
- 目标 schema 定义（~2-4 KB，按需加载）
- replicate-config 摘要（~1 KB）

**定向回读源码**（fallback）：
- 卡片提供 symbols + business_intent → 大部分场景足够
- 需要精确实现细节时（边界条件、算法逻辑、exact 模式）→ 定向读源文件中的具体函数（~20 行），不盲读整个文件
- exact 模式下卡片作为导航图，频繁定向回读

**按保真度回读**：
- interface / functional：卡片 symbols + business_intent 通常足够，极少回读
- architecture：卡片 + code-index 全景，跨切关注需要细节时回读
- exact：卡片作为导航图，频繁定向回读具体函数

**4D 自检适配**：
- D2（证据）追溯链：产物声明 → 卡片 symbol/business_intent → （可选）源码具体行
- 卡片证据充分时无需回读源码；D2 要求精确实现细节时触发定向回读
```

- [ ] **Step 2: Verify the edit**

Run: `grep -n "code-index\|file-catalog\|定向回读\|iron law 26" code-replicate-skill/docs/phase3/standard-artifact-steps.md`
Expected: Multiple matches confirming the new context loading section.

- [ ] **Step 3: Commit**

```bash
git add code-replicate-skill/docs/phase3/standard-artifact-steps.md
git commit -m "docs(code-replicate): replace raw source context with knowledge base in Phase 3"
```

---

### Task 5: Verify all changes are consistent

**Files:**
- Read: all 4 modified files

- [ ] **Step 1: Cross-reference check**

Verify:
1. `schema-reference.md` has both `file-catalog.json` and `code-index.json` sections
2. `stage-a-structure.md` references `schema-reference.md#file-catalog` and `#code-index`
3. `code-replicate-core.md` Stage A table has Step 2.3.7, iron law 26 mentions code-index
4. `standard-artifact-steps.md` context block references code-index.json and file-catalog module slices
5. Iron law 26 in core.md is consistent with the context block in standard-artifact-steps.md
6. Step numbering: 2.3 → 2.3.5 → 2.3.7 → 2.4 (no collision)

Run:
```bash
grep -n "2.3.7" code-replicate-skill/docs/phase2/stage-a-structure.md code-replicate-skill/skills/code-replicate-core.md
grep -n "code-index" code-replicate-skill/skills/code-replicate-core.md code-replicate-skill/docs/phase3/standard-artifact-steps.md
grep -n "file-catalog" code-replicate-skill/docs/schema-reference.md code-replicate-skill/docs/phase2/stage-a-structure.md
```

Expected: Consistent references across all files.

- [ ] **Step 2: Run existing tests to ensure no breakage**

```bash
cd code-replicate-skill/scripts && python3 -m pytest -q
```

Expected: All existing tests pass (no Python scripts were modified).
