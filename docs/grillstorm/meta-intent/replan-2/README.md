# 同步后票据重生成 v2（已批准执行）

main fa2447b20199fa6f8199cdbbe2c05692e283f08c 已合入集成分支 6e509173；保留的 #9 候选同步至 c1893395b8ea49084a6bbb9a63c671868d236a56。main 工作区保持原样，未将未验收代码合回 main，未推送。

## 提议的票与依赖

保留 #9–#18 身份与原父规格 #8，十张独立草案见 issues/。原 30 条需求与 30×2 真实宿主矩阵不减项；旧冻结材料仅作历史，不可按旧任务包继续调度。

| 原票/新版票 | 交付 | 阻塞 |
|---|---|---|
| #9 | 局部需求完整通路，修复历史决定复用；复用候选代码 | 无 |
| #10 | 产品意图确认/补充/调整/删除及完整规划，兼容批量确认 | #9 |
| #11 | 续接、旧基准接入、两端一次性运行策略与产品授权隔离 | #10 |
| #12 | 源码/基准精确失效及发布时输入变更防护 | #10 |
| #13 | 实现—文档—验收闭环，带缺口结束不冒充通过 | #12 |
| #14 | 外部漂移接受/拒绝/暂缓到恢复的完整路径 | #11、#13 |
| #15 | 目标分流和确认：7 场景、14 宿主单元 | #9、#10 |
| #16 | 续接及旧资料：7 场景、14 宿主单元 | #11 |
| #17 | 增量与假完成：8 场景、16 宿主单元 | #12、#13 |
| #18 | 外部恢复：8 场景、16 宿主单元 | #14 |

保留原依赖；#15→#9 和 #17→#12 虽可由传递路径表达，仍显式标注其验收对象，不影响调度前沿。#10 后 #11、#12、#15 可并发；之后依各自产物进入下一前沿，不提前执行尚无生产者实现的验收。

## 与另一批票的边界

- #24、#25 是相关工作，不假设已经落地，不将其实现重复塞入本批。产品确认可整批明确同意，但确认例外机制不得把沉默当批准。
- #27→#40→#41 独立拥有视觉协议迁移。本批保留 ADR-0003，不重做迁移，也不提前要求尚未落地的新文件形态。
- 同文件编辑在集成时串行检查；这类冲突风险不自动成为所有开发工作的业务依赖。
- ADR-0003 的第二视觉上下文回退不豁免本批 Claude/Codex 两个真实宿主的行为验收。
- main 的 2D 能力披露与一次性 Run Policy 不得被旧模板覆盖。

## 重生成时的证据与未完成项（历史快照）

同步无冲突。候选范围测试 106 passed；bootstrap/readiness/reconciliation 回归 36 passed；git diff --check 通过。
公开 journal 复用重现仍失败：两端 bootstrap/readiness 放行，但 decision-input gate 将可信历史 journal 判为 orphan；受保护输入未修改。因此 #9 未完成，其他票未开始，真实宿主矩阵仍为 0/60。142 个脚本测试通过不是全部验收通过，也不是实际双宿主执行。

## 下一步门槛

用户以 `$implement 并发` 批准新版工作与 DAG。原 #9–#18 已原位更新，父 #8 不改；执行协议改为用户明确选择的 implement + orchestration，不重跑历史 grillstorm 设计流程。当前状态见 execution.json；旧冻结任务和失败次数保留为历史，不作为本轮调度或验收证据。#9 从同步后的保留候选继续修复，验收集成后才解锁后继。最后完整测试、独立双轴审查并提交开发分支，不推送、不合 main。

## 执行期再次同步

执行期间 main 又推进至 `a37ca40a548bd748b416b908157223a7287ad720`。
在 #11/#12 候选合入开发分支 `0c10c285` 后，将这次已定位的 main 更新
同步进开发分支，不修改 main，也不重复开票。#24/#25 及 #27→#40→#41
现在已有实际提交：输入声明是可按需扩展的最低集合；确认例外仍不得替代
产品决定的明确授权；视觉报告采用 reviewer-neutral 文件名，缺跨平台 CLI
按 ADR-0003 回退到独立上下文。以上更新由原票实现，本批仅保留并集成。

再次同步前的组合候选通过 382 项 Python 测试、55 项执行引擎测试及两个
核心 Python 文件类型检查；这些不是再次同步后的验收，也不是 60 个真实
宿主单元的证明。同步后的组合候选仍须测试与独立审查。当前进度以
execution.json 为准。

再次同步后的组合候选已通过 387 项 Python 测试和 55 项执行引擎测试；
main 工作区仍干净且为非 bare 仓库。独立审查与真实宿主验收继续分别记录。

当前已交付测试的命令映射见 [validation-commands.md](validation-commands.md)。
旧冻结票据中的拟定测试文件名保留为历史，不再作为本轮执行入口。
