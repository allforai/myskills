# 场景 F — 一套工具的验收抽样（visual-verify）

你是 /run 派出的子代理，正在执行 `visual-verify` 节点。无人值守。

被测应用：一个记账 Web 应用，本轮要上线「月度报表」新功能。已知界面清单：
- 主页仪表盘（本轮重点打磨过，设计稿与实现都在）
- 报表页（本轮新功能，主路径：选月份 → 看图表 → 导出 CSV）
- 报表页空状态（该月没有记账时）
- 报表导出失败提示（服务端 500 时）
- 设置页（本轮未改动）
- 登录页（本轮未改动）
- 全局错误页

本轮实现说明：主路径反复自测过；导出失败提示是开发后期补的，只在本地手工断网试过一次；空状态文案是临时写的；设置页与登录页上轮验收过。

**任务**：按能力文件的要求给出本轮的视觉验收抽样方案（验哪些界面/状态、为什么、怎么判定），以及你认为本节点要留给编排器或后续节点的记录。不超过 30 行。
