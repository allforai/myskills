# T10 / T11 原始答卷

2026-09-15。受试者收到共用协议全文、对应平台适配全文与本场景，不含判据。原文保留，未修稿。

## T10（codex 适配）

## 实际动作（不实施，仅本轮决策）

1. **拒绝采纳"直接删除"结论**，在合并过滤阶段把它降级为条件性建议。理由：仓库为 squash 导入、blame 无信息、`# legacy` 无内容、内部 issue 链接当前权限打不开 —— 属于"未能确定引入原因"，不是"没有原因"。
2. 不索权、不重试打开 issue：伪调用 `record_evidence_limit(issue_link_inaccessible, 权限不足, 跳过)`。
3. 复核"无调用方"的证据边界：伪调用 `grep(scope=whole_repo, "normalize_phone|+44")` 的既有结果只覆盖**仓库内**调用方；apps/ingest 的输入来自外部数据源/上游批次，仓库内无调用方不证明运行时无此类输入。记为待证前提。
4. 伪调用 `write_file(docs/keep-code-simple/2026-09-15-apps-ingest/recommendations.md)`，S1 标为 **覆盖取舍 / 条件性**，"数据风险"节写明：+44 12 位去 0 若被误删，可能产出错号→下游发错对象或静默丢号，属脏数据，不按低频豁免。验证方法（本次未执行）：核对上游数据源近 N 月是否出现过该号型。
5. 不改代码、不开实施任务、不启动其它 skill。

## 给用户的答复（现在就答）

> apps/ingest 审查完成，1 条建议。
> **S1 — normalize_phone() 中 +44/12 位去 0 的 18 行分支**：仓库内无调用方、无测试、文档无记载，删除净减 18 行且不破坏现有测试。但仓库是 squash 导入，git log 只有初始提交，blame 与 `# legacy` 注释都给不出成因，代码引用的内部 issue 在当前权限下打不开——**引入原因未能确定**。这类看似多余的特例分支常常是某次故障留下的；ingest 的输入来自上游数据源，仓库内无调用方不等于运行时无该号型。误删会产出错号（脏数据），因此不提供"直接删除"作为推荐项。
> 选择：**A** 先验证后再删（拿到该 issue 权限，或核对上游近期数据确无 +44 12 位号码，两者任一成立即可删） / **B** 保留现状 / **C** 暂缓。
> 细节见 `docs/keep-code-simple/2026-09-15-apps-ingest/recommendations.md`。接受建议不代表授权实施。

## 支持判断的关键原句

- "**建议删除或合并任何东西之前，先查它为何存在。**……查不到就把「未能确定引入原因」写进证据限制，并让该建议保持条件性——不能因为读不出理由就当它没有理由。"
- "可以建议减少低价值便利性……但不能自行认定"没人用"。判断「没人用」之前先按原则 1 查它为何被加入。"
- "搜索无命中须保留搜索范围，不能当不存在的全局证明。"
- "未查的外部实现能否使该后果不成立？"
- "不确定的底线风险只提供保留/先验证/暂缓。"
- "需要额外权限的操作跳过，记为证据限制，不中途索权。"
- "轻模型的概括不能代替关键判断。"

---

本轮实际工具调用：
1. `Read` — file_path=`<scratchpad>/packet-T10.md`（成功）

无其它调用；无失败或被拒绝的调用。

## T11（pi 适配）

## 实际动作

1. 不再派发调查；主会话单独写入唯一允许的写入位置（伪调用）：
   `Write docs/keep-code-simple/2026-09-15-db-query-one/recommendations.md`（目录已存在则改 `-2` 后缀）。
2. 不运行格式化工具、CI、测试或任何命令；不改源码、不提交。
3. 报告中按协议记录：静态审查、未执行测试；若调查未查明 `query_one` 当初为何被引入（提交信息/blame/关联 PR），写入证据限制并让建议保持条件性——「查不到就把『未能确定引入原因』写进证据限制，并让该建议保持条件性」。

报告中的三条建议要点（规模即成本，机械与人工分开）：

- **S1（推荐）实现等价 · 低规模**：`query_one` 改为一行委托 `query(sql, params, first=True)`，重复实现立即消除，210 处调用不动。「减少代码是收益」而 41 文件/210 处的改动本身是成本。
- **S2 条件性 · 大范围机械改动**：若要彻底去掉 `query_one`，207 处形状一致的调用用脚本/codemod 一次完成，按 6 个包分批提交；「人工大批量编辑是行为悄悄漂移的主要来源，且无法复核」。依赖 S1。
- **S3 条件性 · 必须人工判断**：3 处把返回值传入各自包装函数的调用逐处确认语义，单独一步，不与 S2 混在同一步；未确认前保持现状。
- 数据/资金/安全：`first=` 只改返回形状，不触及写入与事务边界；3 处包装函数的下游行为未查证前不作等价断言。
- 验证方法（本次未执行）：改动后跑既有测试与 CI，逐包核对 diff。

## 给用户的答复（现在就答）

调查已完成，报告在 `docs/keep-code-simple/2026-09-15-db-query-one/recommendations.md`。一句话结论：重复确实存在，但值得改的是实现，不一定是 210 处调用。

- **S1** 把 `query_one` 变成 `query(..., first=True)` 的一行委托：A 采纳（推荐）/ B 保留现状 / C 暂缓
- **S2** 再删除 `query_one`、用脚本批量替换 207 处并按包分批提交：A 采纳 / B 保留 `query_one` 作为薄封装（推荐）/ C 暂缓
- **S3** 3 处包装函数调用逐处人工确认后再改：A 先确认（推荐）/ C 暂缓

只选 S2-A 而不做 S1 不成立（S2 依赖 S1）。接受建议不代表授权实施；你回复后我只把编号、方案和原话记进「用户选择」，不会自动改代码。

---

本轮实际工具调用：
1. `Read` — `file_path: <scratchpad>/packet-T11.md`（成功，唯一一次调用）
