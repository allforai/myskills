# Orientation evidence map

Goal verbatim: “grillstorm”“并发执行”, applied to approved GitHub #8 and implementation/thought-test #9–#18.
Starting ref 81acdddca29ab6dde95a1ab72decc2067f3a0f9a; git status --short empty. Integration child at /Users/aa/orca/workspaces/myskills/grillstorm-meta-intent. No implementation dispatched.

All claims below are material, observed, confidence high, status verified by investigator (independent confirmation pending). Impact is execution/acceptance unless stated otherwise.

- OS-001 Entry/intent: canonical claude/meta-skill/skills/bootstrap/SKILL.md Goal mapping auto-prepends reverse-concept for implement when baseline and game document absent. Codex skills/bootstrap.md reads canonical plus product-inference summary. Observed behavior can overexpand a local request. Intent: intentional protection against missing requirement baselines, not proven user approval. Protected constraint: implementation needs legitimate requirements. Alternative: missing docs requires whole reconstruction; rejected explicitly by #8. Counterfactual: scoped confirmed requirements must support local implementation without full reconstruction. Impact scope/authority.
- OS-002 Shared state/reuse: codex/meta-skill/AGENTS.md identifies canonical shared assets and workflow.json as forward contract. Canonical orchestrator validate_bootstrap.py, validate_unattended_readiness.py, check_artifacts.py, reconcile_bootstrap_workflow.py are existing seams; extend instead of duplicate systems. Intent intentional one planning philosophy; evidence ADR-0001 and Codex adapter canonical-read directive. Counterfactual: separate contracts drift between hosts. Impact architecture.
- OS-003 Approval/runtime: docs/adr/0001* explicitly requires upfront decision_inputs and no interviews during run; approval machinery not redesigned. #8 separates inferred code facts and confirmed desired intent and requires pending dependent work blocked. Intent intentional unattended execution with user authority protected. Alternative auto-confirm high-confidence code is expressly rejected. Impact authority/failure recovery.
- OS-004 Test proof: python3 -m pytest claude/meta-skill/tests/unit -q at starting ref returned 100 passed in 0.28s. Earlier unittest discover at tests root returned 0 tests, exit5 and is NOT a passing check. Existing tests are pytest under tests/unit; scenario prompts/fixtures are separate. Intent: deterministic helpers can be tested but do not prove actual Claude/Codex interactions. Counterfactual: prompt substring assertions cannot establish acceptance. Platform runtime currently unverified, to be probed before launch; unavailable platform prevents thought-test closure.
- OS-005 Dependencies: gh issue list shows #8–#18 open; #9 body declares no dependencies. Approved plan records #10<-9, #11<-10, #12<-10, #13<-12, #14<-11,13, #15<-9,10, #16<-11, #17<-12,13, #18<-14. Native edges must be rechecked before dispatch. Intent vertical contract-consuming slices, not arbitrary parallel layers. Counterfactual launching consumers before verified producers violates contract. Impact execution.
- OS-006 Configuration/Git: git status clean, current main 81acdddc supersedes older conversation commit38864f1; orca status runtime ready 1.4.197. Required official skill status reports missing_required=[]; docs/agents tracker and domain exist. Intent preserve unrelated recent cross-exam changes and original checkout; isolated writers and serialized integration protect user work. Alternative treating conversation commit as latest risks regressing later changes. Actual worker launch capability remains a prelaunch environmental probe, not an assumed pass.

Purpose chain for all work: prevent circular code-derived product approval and stale-evidence completion (#8) -> preserve user direction while limiting work to requested scope -> observable bootstrap/resume fixtures, current revision-bound documents and real platform interactions. User-confirmed source #8. No route chosen until independent critique closes.

## Round 1 corrections (OS-002/003 corrected_pending_confirmation)

OS-002 current semantics clarified: reconciliation is an audit/planning projection, not the run completion authority. Its _scan_report_status starts complete, accepts {} and status-less notes; _artifact_state adopts that JSON status, build_state_index records mtime and node-spec hash, and build_reconciliation_plan chooses keep without a candidate or supersede for a removed complete node. A direct pure-function probe reproduced both {} and {notes: no evidence} as ('complete', []). This can preserve unproven work in planning; it is NOT a valid completion contract. check_artifacts.py is a distinct runtime gate and rejects empty JSON. The discrepancy is a current gap to address within #12/#13/#17, not proof that shared validation already agrees. Existing hashes cover node-spec text, not source content + approved baseline; current artifact metadata contains size/mtime, not the required relevant-input fingerprints.

Reuse also includes internal/commands/journal.md: schema_version 1.0, batches with batch_id/source/topic and decisions question/chosen/rationale/supersedes. Preserve these historical batches while extending confirmation metadata. scripts/check_decision_inputs.py checks path existence and orphan wiring only; it cannot prove a decision is confirmed. New authority checks must feed this existing path and readiness rather than introduce a second journal. Intent classification of the discrepancy: accidental acceptance inconsistency relative to #8's explicit desired contract; historical origin unknown but irrelevant to preserving any justified behavior, because #8 explicitly prohibits existence-only acceptance.

OS-003 sequencing clarified: bootstrap goal mapping can generate reverse-concept for autonomous execution. Its Capability currently requires user conflict resolution before proceeding. bootstrap 3.1 loads planning methodology and writes Node-specs; the run template Phase B hands each Node-spec to the executor. There is no established general confirmed-intent barrier in that existing path. This is a present instruction conflict, not a working front-loaded interview. ADR-0001 and both run templates define the intended precedence: no mid-run interviews; upstream preparation must supply decisions. #10 must move inference/confirmation into interactive bootstrap and leave unresolved dependencies non-executable. Current Codex run_preflight stops the whole run on any readiness failure (exit6); requested per-dependent-work blocking is a behavior gap addressed by #11/#14, not a current capability claim. Historical reason for the retained capability wording is unknown and need not be guessed; its conflict with the governing ADR and explicit #8 is observed.

OS-004 additional baseline: python3 -m pytest codex/meta-skill/test_flow.py codex/meta-skill/test_install.py -q -> 31 passed in 1.26s. Total deterministic baseline 131; no live feature acceptance claimed.

OS-005 native GitHub GET dependencies/blocked_by rechecked for each #8–#18; exactly agrees with the listed edges; snapshots in sources/issue-*.json.

OS-006 current integration untracked docs/grillstorm/ are coordinator-owned control-plane artifacts; original /Users/aa/workspace/myskills remains unchanged. Codex actual read-only worker ctx_1b9dfaf196d3 ran tests; Claude probe ctx_e53bbe574a03 ran pwd and git rev-parse HEAD and completed. Claude CLI 2.1.263 and codex-cli 0.153.4 available. Environment startup/tool execution is proven; actual bootstrap interaction remains future acceptance and is not confused with startup.
