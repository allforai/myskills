# Project Setup Adapter

Load official `setup-matt-pocock-skills` once per repository. This is not a second protocol.

Grillstorm run-root mapping after official setup returns:

- Long-run execution state lives under `docs/grillstorm/<goal>/tasks/`.
- Official `to-spec` and `to-tickets` still publish to the configured tracker.
- Store published spec and ticket IDs/URLs in `tasks/catalog.md`.
