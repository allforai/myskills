# Code Tuner Command For OpenCode

Source command:

- `../../../code-tuner-skill/commands/code-tuner.md`

OpenCode translation:

- Treat `full`, `compliance`, `duplication`, `abstraction`, and `report` as
  workflow modes.
- Parse lifecycle intent from user language and default to `pre-launch`.
- Use `../execution-playbook.md` as the OpenCode-friendly execution contract.
- Use the source command and source references as the canonical rule set.
- Write outputs under `.allforai/code-tuner/` just like the source plugin.

