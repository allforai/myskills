**Inherited (frozen) decisions for this node:** target 「首次响应 4 min → ≤1 min」、20 人团队、日均 800 条 — recorded upstream, not mine to change. Unattended: consume recorded decisions, don't re-decide product choices; the one thing upstream did *not* define is what "首次响应" is measured on, and that gap is a real risk (below).

**Sub-Phase 9 成功指标**（字段：`product-concept.json.success_metrics[]`，每条含 metric / baseline / target / 测量源 / 判定窗口 / 护栏；全部 falsifiable）

1. **S1 主指标 — 首次响应时间 P50**：baseline 4 min（上游记录）；target ≤1 min；窗口＝上线后第 4 周整周、服务时段 8:00–20:00；源＝工单系统「客户消息时间戳→首条人工可见回复时间戳」，按会话首条计算；口径冻结后采集，采集期间不得改定义。
2. **S2 覆盖率（防止 S1 被少数快会话拉低）**：≤1 min 完成的会话占比 ≥70%；≤30s 占比仅作观察，不计分。
3. **S3 反作弊护栏（S1 结构上可被"先自动回一句『已收到』"刷满，必须同门槛判定）**：
   - 草稿零改动直发率 ≥50%；大改/弃用率 ≤20%；
   - 客户同会话内「没回答我的问题」类追问比例 ≤baseline+2pt；
   - 转人工专家/二次响应次数不上升；
   - 7 日同客户同问题重复咨询率不上升。
4. **S4 人工负载**：人均日处理会话数不下降（≥baseline）——不允许用"客服更累"换提速。
5. **S5 质量护栏**：CSAT/好评率不下降（跌 >2pt 即失败）；退款/物流等关键类目抽样人评合格率 ≥90%。
6. **判定规则**：S1 达标但任一护栏越线 → 指标判为**未达成**，登记 `speed_gaming`，回写本子阶段复核（不合并进 aggregate 达标数）。
7. **未定项（不自行决定，留编排器/用户）**：「首次响应」是否含自动回复、是否含非服务时段；CSAT baseline 具体值；判定窗口长度。缺任一项则该条标 `status: "needs_definition"`，**不得用估计值充当 baseline**，不得因此顺带改动上游已确认的 4→1 min 目标。
8. **可选（仅当草稿生成含学习/个性化时）**：需补 `adaptive_systems[]` 完整状态机（dimension↔transition↔behavior 闭环），否则标 `not_applicable`。

**留给编排器/后续节点的记录（本节点未完成项，勿当作已交付）**
- 本次仅产出 `success_metrics`；Sub-Phase 9 的 MVP/post-launch 划分、信息架构、交互原则、`product-definition.md` 均标 `pending`。
- `concept-baseline.json`（mission、`roles[].high_frequency_tasks`、`errc_highlights`）须由最后一个 product-concept 子节点产出，本节点不得代为批准上游未确认的 intent。
- 下游：concept-acceptance 用 `success_metrics` + `errc_highlights` 验收；product-verify 在 MVP 验收复核 S1–S5；若某指标实际不可测，报 gap，**不许回改口径**。
- 若上游 positioning 为 `not_differentiated`，本组指标不得被引作差异化证据。

**Need from main agent:** confirm the "首次响应" measurement definition and CSAT baseline source, or authorize `needs_definition` for S1/S5. No executor handoff warranted unless you want these written into `product-concept.json`.