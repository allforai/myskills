**验收范围**（注意：按能力第10条，宽度/外观/语言/字号这些**轴一律全量**、不抽样；"抽样"只发生在"界面 × 状态"的选择上）

必验（本轮变更或高风险）：
1. 报表页-默认/已加载 — 新功能主路径起点；静态页层
2. 报表页-加载中/图表出现 — 5层之"动态效果"层
3. 报表页-**空状态** — 文案是临时写的 ⇒ 最可能出内容缺陷；归数据完整性(empty 态)
4. 报表页-**导出失败(服务端500)** — 开发后期补的、仅本地断网手测一次 ⇒ 500 分支从未真正跑过；需 Network mock(tier 3)，拿不到就记 UNREACHABLE，**不得算已验证**
5. 报表页-主路径链：选月份→看图表→导出CSV — 若 `interaction-recordings.json` 存在，按第8条跑同一业务流链（复合成里程碑层）
6. 主页仪表盘 — 本轮打磨过 ⇒ 需回归；但其参照是**设计稿**，按边界表属 **ui-forge**，visual-verify 只做"与源应用比对"
回归（上轮已验收，仍须全轴截图留基线）：
7. 设置页　8. 登录页　9. 全局错误页 — 未改动、风险低，但能力要求每屏每轴都拍，不得因"没改"跳过

为什么这么选：风险 = 变更 × 未测。3 是临时文案、4 是只手测过一次的 500 分支，风险最高；1/5 自测多、次之；6/7/8/9 是回归与边界移交。

怎么判定：
- 每案真截图（禁改 DOM，数据只经 ViewModel/网络 mock），app 内读回各轴值，宽度取**代码里的阈值**而非开发者窗口
- reviewer 一 亲看原图 → `visual-review-1.json`；任一 blocker/major 即 `failed_visual_review`
- 每案都有账本条目裁决 + `screenshot-manifest.json`；门 = `python3 ${CLAUDE_PLUGIN_ROOT}/scripts/check_visual_evidence.py --run .allforai/visual-verify --node <id>`
- 最终状态以门为准（passed / refused / failed_visual_review …），不以节点自述为准

留给编排器/后续节点的记录：
- 运行目录清单（surface-inventory / case-matrix / visual-baseline / manifest / evidence-entries / reviews）+ 门命令与实际输出 + 最终 state
- **UNREACHABLE 清单**（尤其 500 导出失败）与 declined 轴值及其理由
- 边界记录：若存在源应用则仪表盘设计还原由 ui-forge 承担、visual-verify 不得重复修；无源应用则该屏在本能力内不可裁
- 前置未满足项：本节点须在 cr-fidelity + product-verify + testforge 全过后、且 ui-forge 之后运行
- 触发 SKIP_HEADLESS / CAPTURE_UNAVAILABLE 时的记录

**需要编排器决定的隐藏前提**：能力硬性要求 visual-verify 的参照是**源应用截图**，而场景只给了"设计稿与实现"。若无源应用，本能力应整体 SKIP（设计稿比对归 ui-forge）；请先确认本轮是否真有源应用，否则 1–6 的判定口径要从"与源应用比对"改为 ui-forge 的"与设计稿比对"，两者不能混用。