# Meta intent launch contract

Launch revision1; spec revision2 / task revision1 / workflow revision2. User approved parent issue8 and implementation/thought tickets9–18; official design confirmed. No new start approval is required. Sources, decisions, program/modules, task catalog, frozen task/DAG/interface/path/acceptance contracts and scenario matrix define scope.

## Execution authority

Implement meta-skill goal routing, provisional intent and user four-operation confirmation, safe resume/legacy admission, content-bound incremental freshness, required document synchronization and external-drift recovery, with canonical Claude/Codex parity. T15–T18 exercise the full parent-and-child 30-scenario x2-host denominator on actual run-owned candidate-loaded Skills; no installed updates or surrogate hosts. SG-01 observed-input-to-publication and SG-02 loaded-candidate applicability remain required public proof. Local synthetic fixtures only, no product/live production calls or paid/destructive external actions.

Spec2/task1 are independently closed; workflow2 is independently closed after repair confirmation. Requirement census R01–R30 and risks/states is the approved target, not completion evidence. Existing baseline186 tests are regression checks, not feature proof.

## Model, concurrency and ownership

THINK/BUILD/VERIFY inherit host-owned Codex configuration as frozen in model-policy.md; no model/effort overrides, downgrade or headless substitution. Actual Claude scenario subjects use the available real Claude host only. Root Orca run owns dispatch, completion, question/reply and admission. Cap3 live workers includes evaluators, subjects and reviewers. Apply tasks/controller.py admission and tasks/dispatch-contract.md before every launch. At most one evaluator with auxiliary capacity reserved; pending subjects/completion reviews precede new ordinary admission. Settled workers release before follow-on stages. Parallel Standards/Spec pair waits for two slots. Recompute dependency-ready set after every confirmed integration, no wave barriers or false resource edges.

Every writer gets its own Orca child worktree from the current integration SHA. Shared paths are merge/replay risks, not writer mutexes. Workers may touch only their exact frozen artifact contract, never docs/grillstorm, other worktrees, main or installed skill/config. Root alone writes this run's control artifacts and serializes integration. Fresh supervisor and reviews see frozen task contracts plus repository state, never executor narrative. Subjects receive raw scenario/candidate/current userturn only, never oracle/task packets.

## Git and recovery

Starting ref81acdddca29ab6dde95a1ab72decc2067f3a0f9a. Integration branch j08069099777/grillstorm-meta-intent at the run-owned worktree. Main had unrelated external visual/marketplace work after starting; preserve it, do not stage/stash/clean/reset or absorb it. Commit only explicit owned paths. No pushes, tracker changes, deployments or remote installs. Merge into the user's branch only after all applicable final gates and a fresh ownership check; if overlapping foreign work prevents safe integration, retain verified integration ref and report precise gate.

Each candidate passes focused public behavioral acceptance, fresh supervisor, independent Standards and Spec, serialized merge, post-merge focused check and grillstorm-confirmed marker. A worker_done, green command or candidate commit alone satisfies no semantic dependency. Infrastructure retries2; business retries2; exhausted task escalates and only transitive dependents skip while independent work continues. Bound retries retain failure; no empty/mock/stale/default success. Product in-scope unforeseen implementation choices use approved intent, existing contracts, small reversible change and strong proof, are recorded in autonomous-decisions.md and revalidated. Missing authority never grants permission.

## Global completion

All ten tasks require actual current evidence. Root runs canonical unit pytest, Codex flow/install pytest, Claude engine Node tests, applicable cross-module real CLI/gates and all four actual-host scenario evidence gates on final relevant candidate inputs. Verify copied runtime helpers and standalone/shared adapter reachability; both engines' current completion agrees. Relevant candidate changes invalidate prior affected host proof and require fresh actual retest. Missing/failed/stale host cells remain unverified, not skip passes.

Then independent fixed-ref Standards/Spec reviews close all blocking findings; execution-report.md and terminal owned commit are durable. Report actual changes, command counts/results, current runtime evidence, autonomous repairs, review ledgers, retry/conflict history, remaining gates and integration SHA. Complete only if all required implementation, runtime and review gates pass. Otherwise after viable branches drain report exact deferred/skipped chains and resume pointer. Do not enter optional cross-exam/audit or handoff unless requested.
