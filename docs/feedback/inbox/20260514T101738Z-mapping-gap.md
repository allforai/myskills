## Anonymous Meta-Skill Feedback

Category: mapping-gap
Meta-skill version: unknown

### Failure Pattern

game-2d-production pipeline has no QA-to-repair feedback loop: 40-qa skills complete on blocker-reported status so orchestrator cannot distinguish environment-blocked from code-bug-blocked QA, and no code-repair skill exists. launch-gap-implementation also runs before QA so QA-discovered code gaps are outside its input scope. Suggested: add game-2d-code-repair skill triggered by qa_status=blocked+code_gaps, reorder or repeat launch-gap-implementation after QA.

### Expected Behavior

The meta-skill should record or repair this class of workflow issue without exposing project-specific information.

### Suggested Fix

Update the relevant skill, bootstrap rule, validator, or orchestrator helper so future projects do not hit the same failure pattern.
