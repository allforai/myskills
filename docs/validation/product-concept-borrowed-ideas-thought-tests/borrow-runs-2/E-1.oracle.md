## Inherited decisions (baseline contract)
- One-shot, unattended `feature-prune` node on a non-game consumer web/mobile product; frequency-first heuristics apply.
- Prior inherited prune of this same gap set landed on **T01/T02 in, T03 in as a justified exception, T04–T08 out**, deliberately leaving budget unused rather than "fill the 15 days" (recorded as a legitimate scope-gate outcome). I treat that as the authoritative contract and reproduce it.
- Capability contract: every gap task gets an explicit decision; ≥1 P0 included; reasons must cite frequency/effort/value; excluded tasks are deferred with reasons, not deleted.

```json
{
  "decisions": [
    {"task_id":"T01","included":true, "tier":"P0","reason":"Core value loop (create/send invoice). Highest frequency (weekly+), 3d; without it the product has no product. Include."},
    {"task_id":"T02","included":true, "tier":"P0","reason":"Flow-completeness: closes create→send→get-paid; consumer maturity bar makes the recovery loop P0 even at weekly frequency. 2d, cheap. Include."},
    {"task_id":"T03","included":true, "tier":"P1","reason":"Explicit exception to frequency-first: one-time, but it is the acquisition/onboarding critical path for the stated target (Excel migrants). 3d. Include; revert to excluded if the Excel-migration premise is unconfirmed."},
    {"task_id":"T04","included":false,"tier":"P2","reason":"Occasional; highest-cost cosmetic (3d). Default template already ships; no workflow/value delta. Defer."},
    {"task_id":"T05","included":false,"tier":"P3","reason":"Preference-only polish (1.5d), 'trending' is not value; zero effect on the money loop. Defer."},
    {"task_id":"T06","included":false,"tier":"P3","reason":"Monthly, largest effort (4d), needs account/portal infra; T02 already covers the dunning loop it claims to help. Defer."},
    {"task_id":"T07","included":false,"tier":"P2","reason":"Set-once config (1.5d); 'accounting demands it' is unverified. Default numbering suffices at launch. Defer."},
    {"task_id":"T08","included":false,"tier":"P2","reason":"'Every day' is a system-triggered notification, not a user task — frequency-first does not promote it. Engagement polish, not core; cheap (1d) but off the critical path. Defer."}
  ]
}
```

**Records for the orchestrator / downstream nodes:**
- Included scope = **T01–T03 = 8 of 15 person-days**; 7d left unused on purpose. Do **not** backfill T06 (4d) just because it fits — that reinstates a low-frequency, high-effort item. If the user wants the headroom spent, T07 or T08 are the cheap candidates; ask.
- **translate / generate-artifacts / ui-design**: implement only T01–T03. Nothing else.
- **product-verify**: cite the T04–T08 inclusion=false reasons when functionality is absent.
- Owed artifacts (capability contract): `prune-tasks.json` (above), `frequency-tier.json` (P0×2, P1×1, P2×3, P3×2), `prune-report.md`. Decision matrix must carry `task_ref` as the FK to `gap-tasks.gaps[].task_ref`.

**Risks / uncertain assumptions:** (1) T03's include hinges entirely on the "coming from Excel" acquisition premise — if false, T03 moves to excluded/P2 and included scope drops to 5d; this is the one include that should be confirmed. (2) Upstream estimates look optimistic (T06 4d for a portal is likely understated) — another reason not to bank on using full budget. (3) T08's tier depends on reading its "daily" as system-frequency; if the frequency annotation is meant as user-task frequency, tier assignment should be revisited explicitly.

**Need from main agent:** confirm the Excel-migration premise for T03 (and whether any unused budget should be spent) before this prune is treated as final.

**Suggested execution prompt:** none — this is a one-shot scope decision; no `worker` implementation handoff is warranted.